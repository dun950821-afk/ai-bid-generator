# Phase 4.7 工作流 API + 前端集成设计文档

> **版本：** 1.1
> **日期：** 2026-05-25
> **状态：** 待评审
> **依赖：** Phase 4.2/4.3（流程模板模型已实现）、Phase 4.5/4.6（解析引擎、提示词管理）

---

## 1. 概述

### 1.1 背景

Phase 4.2/4.3 已完成工作流模型和服务层：
- `WorkflowTemplate`、`WorkflowNodeTemplate` - 流程模板
- `LotWorkflow`、`WorkflowNodeInstance` - 工作流实例
- `TemplateService`、`WorkflowService` - 业务服务

Phase 4.5/4.6 已完成数据处理和 AI 能力：
- 招标文件解析引擎（ParsedDocument、TenderChunk）
- 提示词管理（PromptTemplate、PromptRun）

Phase 4.7 需要补齐 API 层和前端可视化，让用户能：
- 清楚看到标书生成流程执行到哪一步
- 每个节点状态如何
- 产物是什么
- 失败后如何处理

### 1.2 目标

建立 **执行态流程画布 + 节点详情抽屉 + 产物预览 + 工作流 API**：

- **执行态优先**：P0 聚焦执行态，编辑态预留到 P1
- **语义化节点 UI**：数据型/审批型/AI型/手动型/系统型
- **状态联动抽屉**：单一状态源，画布与抽屉自动同步
- **产物预览区**：动态 Viewer，按需加载
- **轻量轮询**：状态轮询 + 详情按需加载

### 1.3 MVP 边界

**P0（核心）：**
- 执行态流程画布（Vue Flow）
- 工作流实例 API（初始化、启动、查询）
- 节点详情 API + 状态机校验
- 节点动作 API：
  - **后端支持**：start / complete / fail / retry / approve / reject
  - **前端暴露**：retry / approve / reject
  - **系统/管理员**：start / complete / fail（由 Celery 任务或管理员调用）
  - skip 暂放 P1 或仅管理员可用
- 节点产物 API（列表、预览、下载）
- 节点日志 API
- 5 类自定义节点 UI
- 右侧抽屉（概览/输入/产物/日志）
- MiniMap + Controls + FitView
- 3-5 秒轻量轮询

**P1（增强）：**
- 编辑模式
- 模板编辑 API
- 节点工具库
- 拖拽新增节点
- 草稿保存与发布
- 快捷搜索节点
- 状态变化 Toast
- 自动定位当前节点

**P2（高级）：**
- rollback（强制 reason + downstream artifact 策略）
- 执行回放
- 并行节点
- 条件分支
- SLA 预警
- 甘特图
- AI 节点动态光圈
- Edge 流动动画

---

## 2. 数据模型

### 2.1 工作流实例

```typescript
interface WorkflowInstance {
  id: string
  lotId: string
  templateId: string
  templateVersion: string
  status: 'pending' | 'running' | 'completed' | 'failed' | 'paused'
  progress: number           // 0-100
  currentNodeId?: string
  startedAt?: string
  completedAt?: string
  updatedAt: string
}
```

### 2.2 节点实例

```typescript
interface WorkflowNodeInstance {
  id: string
  instanceId: string
  templateNodeId: string
  name: string
  nodeType: WorkflowNodeType
  visualType: WorkflowNodeVisualType
  status: WorkflowNodeStatus
  progress: number           // 0-100
  owner?: string
  ownerRole?: string
  metrics?: {
    processedCount?: number
    totalCount?: number
    duration?: number
  }
  startedAt?: string
  completedAt?: string
  errorMessage?: string
  position: { x: number; y: number }
}

type WorkflowNodeType =
  | 'upload'
  | 'parse'
  | 'chunk'
  | 'requirement_extract'
  | 'prompt_run'
  | 'outline_generate'
  | 'section_write'
  | 'approval'
  | 'export'

type WorkflowNodeVisualType =
  | 'data'       // 数据处理：解析、分块、条款抽取
  | 'approval'   // 审批决策：人工审核、确认
  | 'ai'         // AI编排：大模型分析、章节生成
  | 'manual'     // 手动操作：上传资料、补充信息
  | 'system'     // 系统执行：校验、索引、归档

type WorkflowNodeStatus =
  | 'pending'
  | 'running'
  | 'completed'
  | 'failed'
  | 'blocked'
  | 'skipped'
  | 'waiting_approval'
```

### 2.3 边

```typescript
interface WorkflowEdge {
  id: string
  source: string
  target: string
  status?: 'pending' | 'active' | 'completed' | 'failed'
  animated?: boolean
}
```

### 2.4 产物

```typescript
interface WorkflowArtifact {
  id: string
  nodeId: string
  artifactType:
    | 'markdown'
    | 'table'
    | 'json'
    | 'document'
    | 'outline'
    | 'requirements'
    | 'prompt_run'
  title: string
  status: 'generating' | 'ready' | 'failed'
  previewable: boolean
  uri?: string
  data?: unknown
  createdAt: string
}
```

### 2.5 审计日志

```typescript
interface WorkflowAuditLog {
  id: string
  instanceId: string
  nodeId: string
  action: 'start' | 'complete' | 'fail' | 'skip' | 'approve' | 'reject' | 'retry'
  previousStatus: string
  newStatus: string
  operator?: string
  reason?: string
  errorMessage?: string
  createdAt: string
}
```

---

## 3. API 设计

### 3.1 模板 API（P0 只读）

| 端点 | 方法 | 说明 |
|------|------|------|
| `/api/workflows/templates/system/` | GET | 系统模板列表 |

**响应示例：**
```json
{
  "results": [
    {
      "id": 1,
      "name": "工程类投标",
      "description": "适用于工程类投标项目",
      "is_builtin": true,
      "nodes": [
        {"id": 1, "name": "上传招标文件", "order": 1, "visualType": "manual"},
        {"id": 2, "name": "解析招标文件", "order": 2, "visualType": "data"},
        {"id": 3, "name": "AI 条款分析", "order": 3, "visualType": "ai"}
      ]
    }
  ]
}
```

### 3.2 工作流实例 API

| 端点 | 方法 | 说明 |
|------|------|------|
| `/api/workflows/instances/{lot_id}/` | GET | 获取标段工作流（完整） |
| `/api/workflows/instances/{lot_id}/status/` | GET | 轻量状态轮询 |
| `/api/workflows/instances/{lot_id}/initialize/` | POST | 初始化工作流 |
| `/api/workflows/instances/{lot_id}/start/` | POST | 启动工作流 |

**初始化请求：**
```json
{
  "template_id": 1
}
```

**轻量状态响应：**
```json
{
  "instance_id": 1,
  "revision": 18,
  "status": "running",
  "progress": 45,
  "current_node_id": "node_parse",
  "updated_at": "2026-05-25T10:00:00Z",
  "nodes": [
    {
      "id": "node_upload",
      "status": "completed",
      "progress": 100,
      "updated_at": "2026-05-25T09:55:00Z"
    },
    {
      "id": "node_parse",
      "status": "running",
      "progress": 68,
      "updated_at": "2026-05-25T10:00:00Z"
    }
  ]
}
```

**revision 说明：** 每次节点状态、进度、产物变化时递增，前端可用它判断是否需要刷新详情、产物或日志。

### 3.3 节点 API

| 端点 | 方法 | 说明 |
|------|------|------|
| `/api/workflows/nodes/{id}/` | GET | 节点详情 |
| `/api/workflows/nodes/{id}/start/` | POST | 开始执行节点 |
| `/api/workflows/nodes/{id}/complete/` | POST | 完成节点 |
| `/api/workflows/nodes/{id}/skip/` | POST | 跳过节点 |
| `/api/workflows/nodes/{id}/fail/` | POST | 标记失败 |
| `/api/workflows/nodes/{id}/retry/` | POST | 重试节点 |
| `/api/workflows/nodes/{id}/approve/` | POST | 审批通过 |
| `/api/workflows/nodes/{id}/reject/` | POST | 审批驳回 |
| `/api/workflows/nodes/{id}/artifacts/` | GET | 节点产物列表 |
| `/api/workflows/nodes/{id}/logs/` | GET | 节点日志（分页） |

**审批请求：**
```json
{
  "comment": "审核通过，符合要求"
}
```

**驳回请求：**
```json
{
  "comment": "技术方案需要补充实施细节"
}
```

**重试请求：**
```json
{
  "reason": "API 临时超时，重新执行"
}
```

### 3.5 产物 API

| 端点 | 方法 | 说明 |
|------|------|------|
| `/api/workflows/artifacts/{id}/` | GET | 产物详情 |
| `/api/workflows/artifacts/{id}/preview/` | GET | 产物预览数据 |
| `/api/workflows/artifacts/{id}/download/` | GET | 产物下载 |

**权限校验：** 用户必须属于该项目/标段/企业，才能访问产物。产物可能包含招标文件、企业资质、报价策略、PromptRun 输出等敏感内容。

**下载策略：**
- 小文件（< 10MB）：直接 Response
- 大文件（>= 10MB）：返回 MinIO presigned_url

```json
{
  "id": 1,
  "artifact_type": "requirements",
  "title": "条款抽取结果",
  "status": "ready",
  "previewable": true,
  "download_url": null,
  "data": {
    "total": 96,
    "mandatory": 23,
    "optional": 73,
    "items": [...]
  }
}
```

**大文件响应：**
```json
{
  "id": 1,
  "artifact_type": "document",
  "title": "招标文件原文",
  "download_url": "https://minio.example.com/...",
  "expires_at": "2026-05-25T11:00:00Z"
}
```

### 3.6 状态机校验

节点动作必须校验状态迁移合法性：

| 当前状态 | 允许的动作 |
|---------|-----------|
| `pending` | start, skip |
| `running` | complete, fail |
| `failed` | retry |
| `waiting_approval` | approve, reject |
| `completed` | - |
| `skipped` | - |
| `blocked` | unblock, skip（预留） |

非法状态迁移返回 `409 Conflict`：

**自定义异常：**
```python
class StateTransitionError(APIException):
    status_code = 409
    default_code = "invalid_state_transition"
```

**错误响应：**
```json
{
  "error": "invalid_state_transition",
  "message": "节点状态为 completed，无法再次 complete",
  "current_status": "completed",
  "allowed_actions": []
}

---

## 4. 前端设计

### 4.1 组件结构

```
WorkflowBoard.vue
├── WorkflowHeader.vue          # 项目名、流程状态、进度、操作按钮
├── WorkflowToolbar.vue         # 搜索、缩放、全屏
├── WorkflowCanvas.vue          # Vue Flow 主画布
│   ├── nodes/
│   │   ├── DataNode.vue        # 数据节点：进度条、数量指标
│   │   ├── ApprovalNode.vue    # 审批节点：签章、审批状态
│   │   ├── AINode.vue          # AI节点：模型标签
│   │   ├── ManualNode.vue      # 手动节点：用户头像
│   │   └── SystemNode.vue      # 系统节点：齿轮图标
│   └── edges/
│       └── WorkflowEdge.vue    # 流程边
├── NodeDetailDrawer.vue        # 右侧抽屉
│   ├── OverviewTab.vue         # 概览
│   ├── InputTab.vue            # 输入参数
│   ├── ArtifactTab.vue         # 产物列表 + 预览
│   └── LogTab.vue              # 执行日志
└── TemplateSelectDialog.vue    # 初始化时选择模板
```

### 4.2 节点卡片设计

节点卡片内容控制在 4 层：

```
┌────────────────────────────┐
│ AI 条款分析          [AI]  │  <- 名称 + 类型标签
│ ● 运行中  68%              │  <- 状态 + 进度
│ 已分析 42 / 96 条          │  <- 核心指标
│ 负责人：系统               │  <- 负责人
└────────────────────────────┘
```

**状态颜色：**
| 状态 | 颜色 |
|------|------|
| pending | gray |
| running | blue |
| completed | green |
| failed | red |
| waiting_approval | orange |
| blocked | gray-dark |
| skipped | gray-light |

### 4.3 Pinia Store 设计

```typescript
// workflowStore.ts
const workflowStore = defineStore('workflow', () => {
  // 领域数据（后端返回）
  const workflowInstance = ref<WorkflowInstance | null>(null)
  const workflowNodes = ref<WorkflowNodeInstance[]>([])
  const workflowEdges = ref<WorkflowEdge[]>([])
  
  // UI 状态
  const selectedNodeId = ref<string | null>(null)
  const isLoading = ref(false)
  
  // 轮询控制
  let pollingTimer: number | null = null
  
  // 计算属性：选中的节点
  const selectedNode = computed(() =>
    workflowNodes.value.find(n => n.id === selectedNodeId.value)
  )
  
  // 计算属性：Vue Flow 节点（渲染层）
  const flowNodes = computed(() =>
    workflowNodes.value.map(toVueFlowNode)
  )
  
  // 计算属性：Vue Flow 边
  const flowEdges = computed(() =>
    workflowEdges.value.map(toVueFlowEdge)
  )
  
  // 方法
  async function fetchWorkflow(lotId: string) { ... }
  async function fetchStatus(lotId: string) { ... }
  function startPolling(lotId: string) { ... }
  function stopPolling() { ... }
  function selectNode(id: string | null) { ... }
  async function retryNode(nodeId: string, reason: string) { ... }
  async function approveNode(nodeId: string, comment: string) { ... }
  async function rejectNode(nodeId: string, comment: string) { ... }
  function locateNode(nodeId: string) { ... }
  
  return {
    // 状态
    workflowInstance,
    workflowNodes,
    workflowEdges,
    selectedNodeId,
    isLoading,
    // 计算属性
    selectedNode,
    flowNodes,
    flowEdges,
    // 方法
    fetchWorkflow,
    fetchStatus,
    startPolling,
    stopPolling,
    selectNode,
    retryNode,
    approveNode,
    rejectNode,
    locateNode,
  }
})
```

**数据流：**
```
后端轮询 /status
      ↓
workflowStore.fetchStatus()
      ↓
workflowNodes 更新
      ↓
flowNodes computed 自动刷新
      ↓
Vue Flow 节点刷新
      ↓
Drawer selectedNode computed 自动刷新
```

### 4.4 轮询策略

```typescript
let polling = false

async function startPolling(lotId: string) {
  stopPolling()
  
  const poll = async () => {
    if (polling) return  // 防并发重入
    polling = true
    
    try {
      const status = await fetchStatus(lotId)
      
      // 状态变化提示
      if (status.nodeChanged) {
        ElMessage.success(`节点已更新为"${status.newStatus}"`)
      }
      
      // 继续轮询条件
      if (status.status === 'running') {
        pollingTimer = setTimeout(poll, 3000)
      } else if (status.status === 'failed') {
        stopPolling()
      }
    } finally {
      polling = false
    }
  }
  
  poll()
}

function stopPolling() {
  if (pollingTimer) {
    clearTimeout(pollingTimer)
    pollingTimer = null
  }
}

// 组件卸载时停止轮询
onBeforeUnmount(() => stopPolling())

// 页面不可见时暂停轮询
useEventListener(document, 'visibilitychange', () => {
  if (document.hidden) {
    stopPolling()
  } else if (workflowInstance.value?.status === 'running') {
    startPolling(lotId)
  }
})
```

**轮询频率：**
- 运行中：3-5 秒
- 全部完成：停止轮询
- 页面不可见（visibilitychange）：暂停轮询
- 用户查看失败节点：继续刷新日志

### 4.5 抽屉联动

抽屉只存储 `selectedNodeId`，详情数据从 store 或接口获取：

```vue
<template>
  <el-drawer v-model="visible" @close="handleClose">
    <template #header>
      <span>{{ selectedNode?.name }}</span>
      <el-tag :type="getStatusType(selectedNode?.status)">
        {{ getStatusLabel(selectedNode?.status) }}
      </el-tag>
    </template>
    
    <el-tabs v-model="activeTab">
      <el-tab-pane label="概览" name="overview">
        <OverviewTab :node="selectedNode" />
      </el-tab-pane>
      <el-tab-pane label="输入" name="input">
        <InputTab :nodeId="selectedNodeId" />
      </el-tab-pane>
      <el-tab-pane label="产物" name="artifact">
        <ArtifactTab :nodeId="selectedNodeId" />
      </el-tab-pane>
      <el-tab-pane label="日志" name="log">
        <LogTab :nodeId="selectedNodeId" />
      </el-tab-pane>
    </el-tabs>
  </el-drawer>
</template>

<script setup>
const workflowStore = useWorkflowStore()

const selectedNodeId = computed(() => workflowStore.selectedNodeId)
const selectedNode = computed(() => workflowStore.selectedNode)
const visible = computed({
  get: () => !!selectedNodeId.value,
  set: (val) => !val && workflowStore.selectNode(null)
})
</script>
```

### 4.6 产物预览 Viewer 映射

| artifactType | Viewer 组件 |
|-------------|------------|
| `document` | ParsedDocumentViewer |
| `table` | DataTableViewer |
| `requirements` | RequirementTableViewer |
| `outline` | OutlineTreeViewer |
| `markdown` | MarkdownPreview |
| `prompt_run` | PromptRunViewer |
| `json` | JsonCodeViewer |

---

## 5. 后端实现

### 5.1 新增文件

| 文件 | 职责 |
|------|------|
| `backend/apps/workflows/serializers/__init__.py` | 序列化器包 |
| `backend/apps/workflows/serializers/template_serializer.py` | 模板序列化器 |
| `backend/apps/workflows/serializers/workflow_serializer.py` | 工作流序列化器 |
| `backend/apps/workflows/views/__init__.py` | 视图包 |
| `backend/apps/workflows/views/template_views.py` | 模板视图 |
| `backend/apps/workflows/views/workflow_views.py` | 工作流视图 |
| `backend/apps/workflows/views/node_views.py` | 节点视图 |
| `backend/apps/workflows/urls.py` | URL 路由 |
| `backend/apps/workflows/services/audit_service.py` | 审计日志服务 |
| `backend/apps/workflows/models/workflow_audit_log.py` | 审计日志模型 |

### 5.2 审计日志模型

```python
# backend/apps/workflows/models/workflow_audit_log.py
class WorkflowAuditLog(TimeStampedModel):
    """工作流审计日志。"""

    lot_workflow = models.ForeignKey(
        "workflows.LotWorkflow",
        on_delete=models.CASCADE,
        related_name="audit_logs",
        verbose_name="工作流实例",
    )
    node = models.ForeignKey(
        "workflows.WorkflowNodeInstance",
        on_delete=models.CASCADE,
        related_name="audit_logs",
        verbose_name="节点",
    )
    action = models.CharField(
        "动作",
        max_length=32,
    )
    previous_status = models.CharField(
        "变更前状态",
        max_length=32,
    )
    new_status = models.CharField(
        "变更后状态",
        max_length=32,
    )
    operator = models.ForeignKey(
        settings.AUTH_USER_MODEL,
        on_delete=models.SET_NULL,
        null=True,
        blank=True,
        verbose_name="操作人",
    )
    reason = models.TextField(
        "原因/备注",
        blank=True,
    )
    error_message = models.TextField(
        "错误信息",
        blank=True,
    )

    class Meta:
        db_table = "workflow_audit_log"
        verbose_name = "工作流审计日志"
        verbose_name_plural = "工作流审计日志"
        indexes = [
            models.Index(fields=["lot_workflow", "created_at"]),
            models.Index(fields=["node", "created_at"]),
            models.Index(fields=["action"]),
        ]
```

### 5.3 节点动作视图

```python
# backend/apps/workflows/views/node_views.py
from rest_framework.exceptions import APIException


class StateTransitionError(APIException):
    """状态迁移错误。"""
    status_code = 409
    default_code = "invalid_state_transition"


class NodeActionView(APIView):
    """节点动作基类。"""
    
    def validate_state_transition(self, node, action):
        """校验状态迁移合法性。"""
        allowed = STATE_TRANSITIONS.get(node.status, [])
        if action not in allowed:
            raise StateTransitionError({
                "error": "invalid_state_transition",
                "message": f"节点状态为 {node.status}，无法执行 {action}",
                "current_status": node.status,
                "allowed_actions": allowed,
            })
    
    def record_audit(self, node, action, previous_status, new_status, operator, reason=None):
        """记录审计日志。"""
        WorkflowAuditLog.objects.create(
            lot_workflow=node.lot_workflow,
            node=node,
            action=action,
            previous_status=previous_status,
            new_status=new_status,
            operator=operator,
            reason=reason,
        )


class NodeStartView(NodeActionView):
    """开始执行节点。"""
    
    def post(self, request, node_id):
        node = get_object_or_404(WorkflowNodeInstance, pk=node_id)
        self.validate_state_transition(node, 'start')
        
        previous_status = node.status
        WorkflowService.start_node(node, request.user)
        node.refresh_from_db()
        self.record_audit(node, 'start', previous_status, node.status, request.user)
        
        return Response({
            "id": node.id,
            "status": node.status,
        })


class NodeRetryView(NodeActionView):
    """重试节点。"""
    
    def post(self, request, node_id):
        node = get_object_or_404(WorkflowNodeInstance, pk=node_id)
        self.validate_state_transition(node, 'retry')
        
        previous_status = node.status
        reason = request.data.get('reason', '')
        WorkflowService.start_node(node, request.user)
        node.refresh_from_db()
        self.record_audit(node, 'retry', previous_status, node.status, request.user, reason)
        
        return Response({
            "id": node.id,
            "status": node.status,
        })


class NodeApproveView(NodeActionView):
    """审批通过。"""
    
    def post(self, request, node_id):
        node = get_object_or_404(WorkflowNodeInstance, pk=node_id)
        self.validate_state_transition(node, 'approve')
        
        previous_status = node.status
        comment = request.data.get('comment', '')
        WorkflowService.approve_node(node, request.user, comment)
        node.refresh_from_db()
        self.record_audit(node, 'approve', previous_status, node.status, request.user, comment)
        
        return Response({
            "id": node.id,
            "status": node.status,
            "approval_status": node.approval_status,
        })
```

### 5.4 状态迁移表

```python
# backend/apps/workflows/constants.py
STATE_TRANSITIONS = {
    'pending': ['start', 'skip'],
    'running': ['complete', 'fail'],
    'failed': ['retry'],
    'waiting_approval': ['approve', 'reject'],
    'completed': [],
    'skipped': [],
    'blocked': ['unblock', 'skip'],  # 预留
}
```

---

## 6. 实现注意事项

1. **非法状态迁移必须返回 409 Conflict**，使用自定义 `StateTransitionError` 异常，不使用普通 400 ValidationError。
2. **审计日志必须在状态变更前捕获 previous_status**，状态变更后 refresh_from_db() 捕获 new_status。
3. **complete/fail 主要供系统任务或管理员调用**，普通用户 P0 只暴露 retry/approve/reject。
4. **/status/ 轻量轮询接口返回 revision**，用于前端判断是否需要刷新详情、产物或日志。
5. **产物 preview/download 必须校验项目/标段权限**，大文件（>= 10MB）建议返回 MinIO 预签名 URL。
6. **前端轮询需要防并发重入**，并在页面卸载、不可见时暂停。
7. **blocked 状态后续需要支持 unblock 或服务端自动恢复**，P0 可先预留但不开放前端按钮。
8. **Vue Flow 渲染节点应由 workflowNodes 计算生成**，不直接污染领域数据，保持领域层与渲染层分离。

---

## 7. 验收标准

### P0 验收

- [ ] 1. 系统模板列表 API 可用
- [ ] 2. 工作流初始化 API 可用（选择模板）
- [ ] 3. 工作流启动 API 可用
- [ ] 4. 轻量状态轮询 API 可用（含 revision）
- [ ] 5. 节点详情 API 可用
- [ ] 6. 节点动作 API 可用（start/complete/fail/retry/approve/reject）
- [ ] 7. 状态机校验生效（非法迁移返回 409）
- [ ] 8. 审计日志记录正常（previous_status/new_status 正确捕获）
- [ ] 9. WorkflowAuditLog 模型及索引创建
- [ ] 10. 节点产物 API 可用（列表、预览、下载）
- [ ] 11. 产物权限校验生效
- [ ] 12. 节点日志 API 可用（分页）
- [ ] 13. Vue Flow 流程画布正常显示
- [ ] 14. 5 类自定义节点 UI 正常显示
- [ ] 15. 节点点击打开右侧抽屉
- [ ] 16. 抽屉 4 个 Tab 正常切换
- [ ] 17. Pinia 单一状态源正常工作
- [ ] 18. 3-5 秒轮询正常工作（防并发重入）
- [ ] 19. 页面卸载/不可见时停止轮询
- [ ] 20. MiniMap + Controls + FitView 正常
- [ ] 21. 失败节点重试按钮可用
- [ ] 22. 所有测试通过

---

## 修订历史

| 版本 | 日期 | 修订内容 |
|------|------|----------|
| 1.0 | 2026-05-25 | 初始版本 |
| 1.1 | 2026-05-25 | 修正 8 点审核反馈：P0 动作范围明确、409 异常定义、审计日志状态捕获、WorkflowAuditLog 模型、revision 字段、产物权限与大文件、轮询防重入、blocked 状态预留、实现注意事项 |
