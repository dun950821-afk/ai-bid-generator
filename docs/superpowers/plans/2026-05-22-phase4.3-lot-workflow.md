# Phase 4.3: 标段 + 工作流执行 实施计划

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 实现标段（Lot）工作流实例模型（LotWorkflow、WorkflowNodeInstance）、工作流执行服务、节点操作 API，支撑标段级流程执行与审批。

**Architecture:** 新增 LotWorkflow 和 WorkflowNodeInstance 模型；WorkflowService 提供工作流启动、节点执行、自动推进、回退等核心逻辑；支持并发控制。

**Tech Stack:** Django 5 / DRF / Redis / pytest / Celery

---

## File Structure

### 新建文件
- `backend/apps/workflows/models/lot_workflow.py` — LotWorkflow 模型
- `backend/apps/workflows/models/workflow_node_instance.py` — WorkflowNodeInstance 模型
- `backend/apps/workflows/services/workflow_service.py` — 工作流执行服务
- `backend/apps/workflows/serializers/workflow_serializer.py` — 工作流序列化器
- `backend/apps/workflows/views/workflow_views.py` — 工作流视图
- `backend/apps/workflows/tests/test_workflow_model.py` — 工作流模型测试
- `backend/apps/workflows/tests/test_workflow_service.py` — 工作流服务测试
- `backend/apps/workflows/tests/test_workflow_api.py` — 工作流 API 测试

### 修改文件
- `backend/apps/workflows/models/__init__.py` — 导出新模型
- `backend/apps/projects/models/lot.py` — Lot 模型添加 workflow 关联
- `backend/apps/workflows/urls.py` — 新增工作流执行路由

---

## Task 1: 创建 LotWorkflow 模型

**Files:**
- Create: `backend/apps/workflows/models/lot_workflow.py`
- Modify: `backend/apps/workflows/models/__init__.py`

- [ ] **Step 1: 编写 LotWorkflow 模型**

Create `backend/apps/workflows/models/lot_workflow.py`:

```python
from django.db import models

from apps.common.models import TimeStampedModel


class LotWorkflow(TimeStampedModel):
    """标段流程实例。"""

    STATUS_CHOICES = [
        ("not_started", "未开始"),
        ("in_progress", "进行中"),
        ("completed", "已完成"),
        ("failed", "已失败"),
        ("archived", "已归档"),
    ]

    lot = models.OneToOneField(
        "projects.Lot",
        on_delete=models.CASCADE,
        related_name="workflow",
        verbose_name="关联标段",
    )
    workflow_template = models.ForeignKey(
        "workflows.WorkflowTemplate",
        on_delete=models.PROTECT,
        related_name="workflow_instances",
        verbose_name="使用的模板",
    )
    status = models.CharField(
        "状态", max_length=16, choices=STATUS_CHOICES, default="not_started"
    )
    started_at = models.DateTimeField("开始时间", null=True, blank=True)
    completed_at = models.DateTimeField("完成时间", null=True, blank=True)

    class Meta:
        db_table = "lot_workflow"
        ordering = ["-created_at"]
        indexes = [
            models.Index(fields=["lot"]),
            models.Index(fields=["status"]),
        ]

    def __str__(self):
        return f"{self.lot.name} / {self.get_status_display()}"

    @property
    def progress_percentage(self):
        """计算完成百分比。"""
        total = self.nodes.count()
        if total == 0:
            return 0
        completed = self.nodes.filter(status="completed").count()
        return round(completed / total * 100, 1)

    @property
    def current_node(self):
        """获取当前执行节点。"""
        return self.nodes.filter(status="in_progress").first()
```

- [ ] **Step 2: 更新模型包 `__init__.py`**

Edit `backend/apps/workflows/models/__init__.py`:

```python
from .workflow_template import WorkflowTemplate
from .workflow_node_template import WorkflowNodeTemplate
from .lot_workflow import LotWorkflow

__all__ = ["WorkflowTemplate", "WorkflowNodeTemplate", "LotWorkflow"]
```

- [ ] **Step 3: 创建迁移文件**

```bash
cd backend && python manage.py makemigrations workflows --name add_lot_workflow
```

---

## Task 2: 创建 WorkflowNodeInstance 模型

**Files:**
- Create: `backend/apps/workflows/models/workflow_node_instance.py`
- Modify: `backend/apps/workflows/models/__init__.py`
- Modify: `backend/apps/projects/models/lot.py`
- Create: `backend/apps/workflows/tests/test_workflow_model.py`

- [ ] **Step 1: 编写 WorkflowNodeInstance 模型**

Create `backend/apps/workflows/models/workflow_node_instance.py`:

```python
from django.conf import settings
from django.db import models

from apps.common.models import TimeStampedModel


class WorkflowNodeInstance(TimeStampedModel):
    """节点实例（运行时）。"""

    STATUS_CHOICES = [
        ("pending", "待处理"),
        ("in_progress", "进行中"),
        ("completed", "已完成"),
        ("failed", "已失败"),
        ("skipped", "已跳过"),
    ]

    APPROVAL_STATUS_CHOICES = [
        ("not_required", "无需审批"),
        ("pending", "待审批"),
        ("approved", "已通过"),
        ("rejected", "已驳回"),
    ]

    lot_workflow = models.ForeignKey(
        "workflows.LotWorkflow",
        on_delete=models.CASCADE,
        related_name="nodes",
        verbose_name="所属流程实例",
    )
    node_template = models.ForeignKey(
        "workflows.WorkflowNodeTemplate",
        on_delete=models.SET_NULL,
        null=True,
        blank=True,
        related_name="node_instances",
        verbose_name="关联节点模板",
    )

    # 快照字段（从模板拷贝，后续修改不影响）
    name = models.CharField("节点名称", max_length=255)
    order = models.IntegerField("排序序号")
    requires_approval = models.BooleanField("是否需要审批", default=False)

    # 运行时状态
    status = models.CharField(
        "状态", max_length=16, choices=STATUS_CHOICES, default="pending"
    )
    assignee_type = models.CharField(
        "负责人类型", max_length=16, choices=[("role", "角色"), ("user", "用户")], default="role"
    )
    assignee_role = models.CharField("负责人角色", max_length=64, blank=True, default="")
    assignee_user = models.ForeignKey(
        settings.AUTH_USER_MODEL,
        on_delete=models.SET_NULL,
        null=True,
        blank=True,
        related_name="assigned_nodes",
        verbose_name="负责人（用户）",
    )

    # 时间戳
    started_at = models.DateTimeField("开始时间", null=True, blank=True)
    completed_at = models.DateTimeField("完成时间", null=True, blank=True)
    failed_at = models.DateTimeField("失败时间", null=True, blank=True)
    failure_reason = models.TextField("失败原因", blank=True, default="")

    # 审批相关
    approval_status = models.CharField(
        "审批状态", max_length=16, choices=APPROVAL_STATUS_CHOICES, default="not_required"
    )
    approver_type = models.CharField(
        "审批人类型", max_length=16, choices=[("role", "角色"), ("user", "用户")], blank=True, default=""
    )
    approver_role = models.CharField("审批角色", max_length=64, blank=True, default="")
    approver_user = models.ForeignKey(
        settings.AUTH_USER_MODEL,
        on_delete=models.SET_NULL,
        null=True,
        blank=True,
        related_name="approved_nodes",
        verbose_name="审批人（用户）",
    )
    approved_by = models.ForeignKey(
        settings.AUTH_USER_MODEL,
        on_delete=models.SET_NULL,
        null=True,
        blank=True,
        related_name="approved_node_instances",
        verbose_name="实际审批人",
    )
    approved_at = models.DateTimeField("审批时间", null=True, blank=True)
    approval_comment = models.TextField("审批意见", blank=True, default="")

    # 重试计数
    retry_count = models.IntegerField("重试次数", default=0)

    class Meta:
        db_table = "workflow_node_instance"
        ordering = ["order", "id"]
        indexes = [
            models.Index(fields=["lot_workflow", "order"]),
            models.Index(fields=["status"]),
        ]

    def __str__(self):
        return f"{self.lot_workflow.lot.name} / {self.name}"

    def can_start(self):
        """检查是否可以开始执行。"""
        if self.status not in ["pending", "failed"]:
            return False, "节点状态不允许开始"

        # 检查前置节点
        prev_nodes = WorkflowNodeInstance.objects.filter(
            lot_workflow=self.lot_workflow,
            order__lt=self.order,
        ).exclude(status="skipped")

        for node in prev_nodes:
            if node.status != "completed":
                return False, f"前置节点「{node.name}」未完成"

        return True, ""

    def can_complete(self):
        """检查是否可以完成。"""
        if self.status != "in_progress":
            return False, "节点未在执行中"

        if self.requires_approval and self.approval_status != "approved":
            return False, "节点需要审批通过后才能完成"

        return True, ""
```

- [ ] **Step 2: 更新模型包 `__init__.py`**

Edit `backend/apps/workflows/models/__init__.py`:

```python
from .workflow_template import WorkflowTemplate
from .workflow_node_template import WorkflowNodeTemplate
from .lot_workflow import LotWorkflow
from .workflow_node_instance import WorkflowNodeInstance

__all__ = [
    "WorkflowTemplate",
    "WorkflowNodeTemplate",
    "LotWorkflow",
    "WorkflowNodeInstance",
]
```

- [ ] **Step 3: 修改 Lot 模型添加 workflow_status 字段**

Read `backend/apps/projects/models/lot.py` and modify:

```python
from django.db import models

from apps.common.models import TimeStampedModel


class Lot(TimeStampedModel):
    """标段。"""

    STATUS_CHOICES = [
        ("active", "进行中"),
        ("archived", "已归档"),
    ]

    WORKFLOW_STATUS_CHOICES = [
        ("not_started", "未开始"),
        ("in_progress", "进行中"),
        ("completed", "已完成"),
        ("archived", "已归档"),
    ]

    project = models.ForeignKey(
        "projects.Project", on_delete=models.CASCADE, related_name="lots", verbose_name="项目"
    )
    name = models.CharField("标段名称", max_length=255)
    code = models.CharField("标段编号", max_length=64, blank=True)
    status = models.CharField(
        "状态", max_length=32, choices=STATUS_CHOICES, default="active"
    )
    workflow_status = models.CharField(
        "流程状态", max_length=16, choices=WORKFLOW_STATUS_CHOICES, default="not_started"
    )

    class Meta:
        db_table = "projects_lot"
        ordering = ["id"]
        indexes = [
            models.Index(fields=["project"]),
            models.Index(fields=["workflow_status"]),
        ]

    def __str__(self):
        return f"{self.project.name} / {self.name}"

    @property
    def has_workflow(self):
        """检查是否已创建工作流。"""
        return hasattr(self, "workflow") and self.workflow is not None
```

- [ ] **Step 4: 编写模型测试**

Create `backend/apps/workflows/tests/test_workflow_model.py`:

```python
import pytest
from django.utils import timezone
from apps.workflows.models import LotWorkflow, WorkflowNodeInstance
from apps.projects.models import Lot


@pytest.mark.django_db
def test_create_lot_workflow(lot_factory, workflow_template_factory):
    """测试创建标段工作流。"""
    lot = lot_factory()
    template = workflow_template_factory(scope="project", project=lot.project)

    workflow = LotWorkflow.objects.create(
        lot=lot,
        workflow_template=template,
    )

    assert workflow.id is not None
    assert workflow.status == "not_started"
    assert workflow.lot == lot


@pytest.mark.django_db
def test_progress_percentage(lot_factory, workflow_template_factory):
    """测试进度计算。"""
    lot = lot_factory()
    template = workflow_template_factory(scope="project", project=lot.project)

    workflow = LotWorkflow.objects.create(
        lot=lot,
        workflow_template=template,
    )

    # 创建 3 个节点
    node1 = WorkflowNodeInstance.objects.create(
        lot_workflow=workflow,
        name="节点1",
        order=1,
        status="completed",
    )
    node2 = WorkflowNodeInstance.objects.create(
        lot_workflow=workflow,
        name="节点2",
        order=2,
        status="in_progress",
    )
    node3 = WorkflowNodeInstance.objects.create(
        lot_workflow=workflow,
        name="节点3",
        order=3,
        status="pending",
    )

    assert workflow.progress_percentage == 33.3


@pytest.mark.django_db
def test_current_node(lot_factory, workflow_template_factory):
    """测试获取当前节点。"""
    lot = lot_factory()
    template = workflow_template_factory(scope="project", project=lot.project)

    workflow = LotWorkflow.objects.create(
        lot=lot,
        workflow_template=template,
    )

    # 没有进行中的节点
    assert workflow.current_node is None

    # 创建节点
    node1 = WorkflowNodeInstance.objects.create(
        lot_workflow=workflow,
        name="节点1",
        order=1,
        status="completed",
    )
    node2 = WorkflowNodeInstance.objects.create(
        lot_workflow=workflow,
        name="节点2",
        order=2,
        status="in_progress",
    )

    assert workflow.current_node == node2


@pytest.mark.django_db
def test_node_can_start(lot_factory, workflow_template_factory):
    """测试节点是否可以开始执行。"""
    lot = lot_factory()
    template = workflow_template_factory(scope="project", project=lot.project)

    workflow = LotWorkflow.objects.create(
        lot=lot,
        workflow_template=template,
    )

    node1 = WorkflowNodeInstance.objects.create(
        lot_workflow=workflow,
        name="节点1",
        order=1,
        status="pending",
    )
    node2 = WorkflowNodeInstance.objects.create(
        lot_workflow=workflow,
        name="节点2",
        order=2,
        status="pending",
    )

    # 第一个节点可以开始
    can, msg = node1.can_start()
    assert can is True

    # 第二个节点不能开始（前置节点未完成）
    can, msg = node2.can_start()
    assert can is False
    assert "节点1" in msg


@pytest.mark.django_db
def test_node_can_complete_with_approval(lot_factory, workflow_template_factory):
    """测试需要审批的节点完成条件。"""
    lot = lot_factory()
    template = workflow_template_factory(scope="project", project=lot.project)

    workflow = LotWorkflow.objects.create(
        lot=lot,
        workflow_template=template,
    )

    node = WorkflowNodeInstance.objects.create(
        lot_workflow=workflow,
        name="审批节点",
        order=1,
        status="in_progress",
        requires_approval=True,
        approval_status="pending",
    )

    # 未审批不能完成
    can, msg = node.can_complete()
    assert can is False
    assert "审批" in msg

    # 审批通过后可以完成
    node.approval_status = "approved"
    can, msg = node.can_complete()
    assert can is True
```

- [ ] **Step 5: 创建迁移文件**

```bash
cd backend && python manage.py makemigrations workflows projects --name add_workflow_instance
```

- [ ] **Step 6: 运行测试**

```bash
cd backend && pytest apps/workflows/tests/test_workflow_model.py -v
```

Expected: PASS

---

## Task 3: 创建工作流执行服务

**Files:**
- Create: `backend/apps/workflows/services/workflow_service.py`
- Create: `backend/apps/workflows/tests/test_workflow_service.py`

- [ ] **Step 1: 编写工作流执行服务**

Create `backend/apps/workflows/services/workflow_service.py`:

```python
"""工作流执行服务。"""
from django.db import transaction
from django.utils import timezone
from django.core.exceptions import ValidationError

from apps.workflows.models import LotWorkflow, WorkflowNodeInstance
from apps.workflows.services.template_service import TemplateService


class WorkflowService:
    """工作流执行服务。"""

    @staticmethod
    @transaction.atomic
    def initialize_workflow(lot, workflow_template=None):
        """初始化标段工作流。

        Args:
            lot: 标段实例
            workflow_template: 流程模板（可选，默认使用项目的第一个模板）

        Returns:
            创建的工作流实例
        """
        if lot.has_workflow:
            raise ValidationError("标段已存在工作流")

        # 获取模板
        if workflow_template is None:
            workflow_template = lot.project.workflow_templates.first()

        if workflow_template is None:
            raise ValidationError("项目没有可用的流程模板")

        # 创建工作流实例
        workflow = LotWorkflow.objects.create(
            lot=lot,
            workflow_template=workflow_template,
        )

        # 创建节点实例
        node_instances = []
        for node_template in workflow_template.node_templates.all().order_by("order"):
            node_instances.append(WorkflowNodeInstance(
                lot_workflow=workflow,
                node_template=node_template,
                name=node_template.name,
                order=node_template.order,
                requires_approval=node_template.requires_approval,
                assignee_type=node_template.default_assignee_type,
                assignee_role=node_template.default_assignee_role,
                approval_status="pending" if node_template.requires_approval else "not_required",
                approver_type=node_template.approver_type,
                approver_role=node_template.approver_role,
            ))

        WorkflowNodeInstance.objects.bulk_create(node_instances)

        return workflow

    @staticmethod
    @transaction.atomic
    def start_workflow(workflow, operator):
        """启动工作流。

        Args:
            workflow: 工作流实例
            operator: 操作人

        Returns:
            更新后的工作流
        """
        if workflow.status != "not_started":
            raise ValidationError("工作流状态不允许启动")

        workflow.status = "in_progress"
        workflow.started_at = timezone.now()
        workflow.save()

        # 启动第一个节点
        first_node = workflow.nodes.order_by("order").first()
        if first_node:
            WorkflowService.start_node(first_node, operator)

        return workflow

    @staticmethod
    @transaction.atomic
    def start_node(node, operator, assignee_user=None):
        """开始执行节点。

        Args:
            node: 节点实例
            operator: 操作人
            assignee_user: 指定负责人（可选）

        Returns:
            更新后的节点
        """
        can_start, reason = node.can_start()
        if not can_start:
            raise ValidationError(reason)

        node.status = "in_progress"
        node.started_at = timezone.now()
        node.retry_count += 1

        if assignee_user:
            node.assignee_user = assignee_user
            node.assignee_type = "user"

        node.save()

        return node

    @staticmethod
    @transaction.atomic
    def complete_node(node, operator):
        """完成节点。

        Args:
            node: 节点实例
            operator: 操作人

        Returns:
            更新后的节点
        """
        can_complete, reason = node.can_complete()
        if not can_complete:
            raise ValidationError(reason)

        node.status = "completed"
        node.completed_at = timezone.now()
        node.save()

        # 自动推进到下一节点
        WorkflowService._auto_advance(node.lot_workflow)

        return node

    @staticmethod
    @transaction.atomic
    def fail_node(node, operator, reason=""):
        """标记节点失败。

        Args:
            node: 节点实例
            operator: 操作人
            reason: 失败原因

        Returns:
            更新后的节点
        """
        if node.status != "in_progress":
            raise ValidationError("只有进行中的节点可以标记失败")

        node.status = "failed"
        node.failed_at = timezone.now()
        node.failure_reason = reason
        node.save()

        return node

    @staticmethod
    @transaction.atomic
    def skip_node(node, operator, reason=""):
        """跳过节点。

        Args:
            node: 节点实例
            operator: 操作人
            reason: 跳过原因

        Returns:
            更新后的节点
        """
        if node.status not in ["pending", "failed"]:
            raise ValidationError("节点状态不允许跳过")

        node.status = "skipped"
        node.completed_at = timezone.now()
        node.failure_reason = reason
        node.save()

        # 自动推进
        WorkflowService._auto_advance(node.lot_workflow)

        return node

    @staticmethod
    @transaction.atomic
    def approve_node(node, operator, comment=""):
        """审批通过节点。

        Args:
            node: 节点实例
            operator: 审批人
            comment: 审批意见

        Returns:
            更新后的节点
        """
        if not node.requires_approval:
            raise ValidationError("该节点无需审批")

        if node.approval_status != "pending":
            raise ValidationError("节点不在待审批状态")

        # 检查审批自我回避
        if node.assignee_type == "user" and node.assignee_user == operator:
            raise ValidationError("不可审批自己负责的节点")
        if node.assignee_type == "role":
            from apps.projects.models import ProjectMember
            member = ProjectMember.objects.filter(
                project=node.lot_workflow.lot.project,
                user=operator
            ).first()
            if member and member.project_role.code == node.assignee_role:
                raise ValidationError("不可审批自己负责角色的节点")

        node.approval_status = "approved"
        node.approved_by = operator
        node.approved_at = timezone.now()
        node.approval_comment = comment
        node.save()

        return node

    @staticmethod
    @transaction.atomic
    def reject_node(node, operator, comment=""):
        """审批驳回节点。

        Args:
            node: 节点实例
            operator: 审批人
            comment: 驳回原因

        Returns:
            更新后的节点
        """
        if not node.requires_approval:
            raise ValidationError("该节点无需审批")

        if node.approval_status != "pending":
            raise ValidationError("节点不在待审批状态")

        node.approval_status = "rejected"
        node.approved_by = operator
        node.approved_at = timezone.now()
        node.approval_comment = comment
        node.save()

        return node

    @staticmethod
    @transaction.atomic
    def rollback_to_node(workflow, target_node_id, operator, reason=""):
        """回退到指定节点。

        Args:
            workflow: 工作流实例
            target_node_id: 目标节点 ID
            operator: 操作人
            reason: 回退原因

        Returns:
            更新后的工作流
        """
        target_node = workflow.nodes.filter(pk=target_node_id).first()
        if not target_node:
            raise ValidationError("目标节点不存在")

        # 锁定下游节点
        downstream_nodes = workflow.nodes.filter(
            order__gte=target_node.order
        ).select_for_update()

        # 重置状态
        for node in downstream_nodes:
            node.status = "pending"
            node.started_at = None
            node.completed_at = None
            node.failed_at = None
            node.failure_reason = ""
            if node.requires_approval:
                node.approval_status = "pending"
            node.retry_count = 0
            node.save()

        # 更新工作流状态
        workflow.status = "in_progress"
        workflow.completed_at = None
        workflow.save()

        return workflow

    @staticmethod
    def _auto_advance(workflow):
        """自动推进流程。

        检查是否所有并行节点完成，推进到下一批节点。
        """
        # 检查是否有进行中的节点
        in_progress = workflow.nodes.filter(status="in_progress")
        if in_progress.exists():
            return

        # 检查是否有失败的节点
        failed = workflow.nodes.filter(status="failed")
        if failed.exists():
            workflow.status = "failed"
            workflow.save()
            return

        # 找到下一个待处理的节点
        pending_nodes = workflow.nodes.filter(status="pending").order_by("order")

        if not pending_nodes.exists():
            # 所有节点完成
            workflow.status = "completed"
            workflow.completed_at = timezone.now()
            workflow.save()

            # 更新标段状态
            lot = workflow.lot
            lot.workflow_status = "completed"
            lot.save()
            return

        # 获取下一批节点（相同 order 的并行节点）
        next_order = pending_nodes.first().order
        batch_nodes = [n for n in pending_nodes if n.order == next_order]

        for node in batch_nodes:
            node.status = "in_progress"
            node.started_at = timezone.now()
            node.save()
```

- [ ] **Step 2: 编写工作流服务测试**

Create `backend/apps/workflows/tests/test_workflow_service.py`:

```python
import pytest
from django.core.exceptions import ValidationError
from django.utils import timezone

from apps.workflows.models import LotWorkflow, WorkflowNodeInstance
from apps.workflows.services.workflow_service import WorkflowService
from apps.projects.models import ProjectMember


@pytest.fixture
def setup_workflow(lot_factory, workflow_template_factory, user_factory):
    """创建完整的工作流环境。"""
    user = user_factory()
    lot = lot_factory()
    template = workflow_template_factory(scope="project", project=lot.project)

    # 创建模板节点
    from apps.workflows.models import WorkflowNodeTemplate
    WorkflowNodeTemplate.objects.create(
        workflow_template=template, name="节点1", order=1,
        default_assignee_role="editor"
    )
    WorkflowNodeTemplate.objects.create(
        workflow_template=template, name="节点2", order=2,
        default_assignee_role="owner", requires_approval=True, approver_role="reviewer"
    )
    WorkflowNodeTemplate.objects.create(
        workflow_template=template, name="节点3", order=3,
        default_assignee_role="owner"
    )

    return user, lot, template


@pytest.mark.django_db
def test_initialize_workflow(setup_workflow):
    """测试初始化工作流。"""
    user, lot, template = setup_workflow

    workflow = WorkflowService.initialize_workflow(lot, template)

    assert workflow.id is not None
    assert workflow.status == "not_started"
    assert workflow.nodes.count() == 3


@pytest.mark.django_db
def test_initialize_workflow_already_exists(setup_workflow):
    """测试重复初始化工作流。"""
    user, lot, template = setup_workflow

    WorkflowService.initialize_workflow(lot, template)

    with pytest.raises(ValidationError):
        WorkflowService.initialize_workflow(lot, template)


@pytest.mark.django_db
def test_start_workflow(setup_workflow):
    """测试启动工作流。"""
    user, lot, template = setup_workflow

    workflow = WorkflowService.initialize_workflow(lot, template)
    WorkflowService.start_workflow(workflow, user)

    workflow.refresh_from_db()
    assert workflow.status == "in_progress"
    assert workflow.started_at is not None

    # 第一个节点应该开始执行
    first_node = workflow.nodes.order_by("order").first()
    assert first_node.status == "in_progress"


@pytest.mark.django_db
def test_complete_node(setup_workflow):
    """测试完成节点。"""
    user, lot, template = setup_workflow

    workflow = WorkflowService.initialize_workflow(lot, template)
    WorkflowService.start_workflow(workflow, user)

    first_node = workflow.nodes.order_by("order").first()
    WorkflowService.complete_node(first_node, user)

    first_node.refresh_from_db()
    assert first_node.status == "completed"

    # 第二个节点应该自动开始
    second_node = workflow.nodes.get(order=2)
    assert second_node.status == "in_progress"


@pytest.mark.django_db
def test_complete_node_with_approval(setup_workflow):
    """测试需要审批的节点完成。"""
    user, lot, template = setup_workflow

    workflow = WorkflowService.initialize_workflow(lot, template)
    WorkflowService.start_workflow(workflow, user)

    # 完成第一个节点
    first_node = workflow.nodes.order_by("order").first()
    WorkflowService.complete_node(first_node, user)

    # 第二个节点需要审批
    second_node = workflow.nodes.get(order=2)
    assert second_node.requires_approval is True

    # 尝试直接完成应该失败
    with pytest.raises(ValidationError):
        WorkflowService.complete_node(second_node, user)


@pytest.mark.django_db
def test_approve_node(setup_workflow, project_factory):
    """测试审批通过节点。"""
    user, lot, template = setup_workflow

    # 初始化项目和角色
    from apps.projects.services.role_service import RoleService
    roles = RoleService.initialize_builtin_roles(lot.project)
    reviewer_role = next(r for r in roles if r.code == "reviewer")
    ProjectMember.objects.create(project=lot.project, user=user, project_role=reviewer_role)

    workflow = WorkflowService.initialize_workflow(lot, template)
    WorkflowService.start_workflow(workflow, user)

    # 完成第一个节点
    first_node = workflow.nodes.order_by("order").first()
    WorkflowService.complete_node(first_node, user)

    # 审批第二个节点
    second_node = workflow.nodes.get(order=2)
    WorkflowService.approve_node(second_node, user, "通过")

    second_node.refresh_from_db()
    assert second_node.approval_status == "approved"
    assert second_node.approved_by == user


@pytest.mark.django_db
def test_approve_own_node_fails(setup_workflow, project_factory):
    """测试审批自己负责的节点应该失败。"""
    user, lot, template = setup_workflow

    # 创建 owner 角色
    from apps.projects.services.role_service import RoleService
    roles = RoleService.initialize_builtin_roles(lot.project)
    owner_role = next(r for r in roles if r.code == "owner")
    ProjectMember.objects.create(project=lot.project, user=user, project_role=owner_role)

    # 修改第二个节点的负责人角色为 owner
    from apps.workflows.models import WorkflowNodeTemplate
    node_template = template.node_templates.get(order=2)
    node_template.default_assignee_role = "owner"
    node_template.save()

    workflow = WorkflowService.initialize_workflow(lot, template)
    WorkflowService.start_workflow(workflow, user)

    # 完成第一个节点
    first_node = workflow.nodes.order_by("order").first()
    WorkflowService.complete_node(first_node, user)

    # 尝试审批自己负责的节点
    second_node = workflow.nodes.get(order=2)
    with pytest.raises(ValidationError, match="自己负责"):
        WorkflowService.approve_node(second_node, user)


@pytest.mark.django_db
def test_fail_node(setup_workflow):
    """测试标记节点失败。"""
    user, lot, template = setup_workflow

    workflow = WorkflowService.initialize_workflow(lot, template)
    WorkflowService.start_workflow(workflow, user)

    first_node = workflow.nodes.order_by("order").first()
    WorkflowService.fail_node(first_node, user, "测试失败")

    first_node.refresh_from_db()
    assert first_node.status == "failed"
    assert first_node.failure_reason == "测试失败"


@pytest.mark.django_db
def test_skip_node(setup_workflow):
    """测试跳过节点。"""
    user, lot, template = setup_workflow

    workflow = WorkflowService.initialize_workflow(lot, template)
    WorkflowService.start_workflow(workflow, user)

    first_node = workflow.nodes.order_by("order").first()
    WorkflowService.skip_node(first_node, user, "跳过测试")

    first_node.refresh_from_db()
    assert first_node.status == "skipped"

    # 第二个节点应该自动开始
    second_node = workflow.nodes.get(order=2)
    assert second_node.status == "in_progress"


@pytest.mark.django_db
def test_rollback_to_node(setup_workflow):
    """测试回退到指定节点。"""
    user, lot, template = setup_workflow

    workflow = WorkflowService.initialize_workflow(lot, template)
    WorkflowService.start_workflow(workflow, user)

    # 完成第一个节点
    first_node = workflow.nodes.order_by("order").first()
    WorkflowService.complete_node(first_node, user)

    # 回退到第一个节点
    WorkflowService.rollback_to_node(workflow, first_node.id, user, "测试回退")

    first_node.refresh_from_db()
    assert first_node.status == "pending"
    assert first_node.started_at is None


@pytest.mark.django_db
def test_workflow_completion(setup_workflow):
    """测试工作流完成。"""
    user, lot, template = setup_workflow

    # 简化模板：移除审批要求
    for node_template in template.node_templates.all():
        node_template.requires_approval = False
        node_template.save()

    workflow = WorkflowService.initialize_workflow(lot, template)
    WorkflowService.start_workflow(workflow, user)

    # 完成所有节点
    for node in workflow.nodes.order_by("order"):
        node.status = "in_progress"
        node.started_at = timezone.now()
        node.save()
        WorkflowService.complete_node(node, user)

    workflow.refresh_from_db()
    assert workflow.status == "completed"
    assert workflow.completed_at is not None

    lot.refresh_from_db()
    assert lot.workflow_status == "completed"
```

- [ ] **Step 3: 运行测试**

```bash
cd backend && pytest apps/workflows/tests/test_workflow_service.py -v
```

Expected: PASS

- [ ] **Step 4: 提交**

```bash
git add backend/apps/workflows/services/workflow_service.py backend/apps/workflows/tests/test_workflow_service.py
git commit -m "feat(workflows): 新增工作流执行服务

- initialize_workflow: 初始化标段工作流
- start_workflow: 启动工作流
- start_node/complete_node/fail_node/skip_node: 节点操作
- approve_node/reject_node: 审批操作（含自我回避检查）
- rollback_to_node: 回退到指定节点
- _auto_advance: 自动推进流程

Co-Authored-By: Claude Opus 4.7 <noreply@anthropic.com>"
```

---

## Task 4: 创建工作流序列化器和视图

**Files:**
- Create: `backend/apps/workflows/serializers/workflow_serializer.py`
- Create: `backend/apps/workflows/views/workflow_views.py`
- Modify: `backend/apps/workflows/urls.py`
- Create: `backend/apps/workflows/tests/test_workflow_api.py`

- [ ] **Step 1: 编写工作流序列化器**

Create `backend/apps/workflows/serializers/workflow_serializer.py`:

```python
from rest_framework import serializers

from apps.workflows.models import LotWorkflow, WorkflowNodeInstance


class WorkflowNodeInstanceSerializer(serializers.ModelSerializer):
    """节点实例序列化器。"""

    assignee_user_name = serializers.CharField(source="assignee_user.real_name", read_only=True)
    approver_user_name = serializers.CharField(source="approver_user.real_name", read_only=True)
    approved_by_name = serializers.CharField(source="approved_by.real_name", read_only=True)

    class Meta:
        model = WorkflowNodeInstance
        fields = [
            "id", "name", "order",
            "status", "requires_approval",
            "assignee_type", "assignee_role", "assignee_user", "assignee_user_name",
            "started_at", "completed_at", "failed_at", "failure_reason",
            "approval_status", "approver_type", "approver_role", "approver_user",
            "approved_by", "approved_by_name", "approved_at", "approval_comment",
            "retry_count",
            "created_at", "updated_at",
        ]
        read_only_fields = [
            "id", "name", "order", "requires_approval",
            "status", "started_at", "completed_at", "failed_at", "failure_reason",
            "approval_status", "approved_by", "approved_at", "approval_comment",
            "retry_count", "created_at", "updated_at",
        ]


class LotWorkflowSerializer(serializers.ModelSerializer):
    """工作流实例序列化器。"""

    nodes = WorkflowNodeInstanceSerializer(many=True, read_only=True)
    template_name = serializers.CharField(source="workflow_template.name", read_only=True)
    progress_percentage = serializers.FloatField(read_only=True)
    current_node_name = serializers.CharField(source="current_node.name", read_only=True)

    class Meta:
        model = LotWorkflow
        fields = [
            "id", "lot", "workflow_template", "template_name",
            "status", "started_at", "completed_at",
            "progress_percentage", "current_node_name",
            "nodes",
            "created_at", "updated_at",
        ]
        read_only_fields = [
            "id", "lot", "workflow_template",
            "status", "started_at", "completed_at",
            "created_at", "updated_at",
        ]


class LotWorkflowDetailSerializer(LotWorkflowSerializer):
    """工作流详情序列化器（含更多节点信息）。"""

    lot_name = serializers.CharField(source="lot.name", read_only=True)
    project_id = serializers.IntegerField(source="lot.project.id", read_only=True)
    project_name = serializers.CharField(source="lot.project.name", read_only=True)


class NodeStartSerializer(serializers.Serializer):
    """开始节点序列化器。"""

    assignee_user_id = serializers.IntegerField(required=False, allow_null=True)


class NodeCompleteSerializer(serializers.Serializer):
    """完成节点序列化器。"""

    pass


class NodeFailSerializer(serializers.Serializer):
    """标记节点失败序列化器。"""

    reason = serializers.CharField(required=False, allow_blank=True, default="")


class NodeSkipSerializer(serializers.Serializer):
    """跳过节点序列化器。"""

    reason = serializers.CharField(required=False, allow_blank=True, default="")


class NodeApproveSerializer(serializers.Serializer):
    """审批通过序列化器。"""

    comment = serializers.CharField(required=False, allow_blank=True, default="")


class NodeRejectSerializer(serializers.Serializer):
    """审批驳回序列化器。"""

    comment = serializers.CharField(required=True, allow_blank=False)


class NodeReassignSerializer(serializers.Serializer):
    """重新指派序列化器。"""

    assignee_type = serializers.ChoiceField(choices=[("role", "角色"), ("user", "用户")])
    assignee_role = serializers.CharField(required=False, allow_blank=True, default="")
    assignee_user_id = serializers.IntegerField(required=False, allow_null=True)


class WorkflowRollbackSerializer(serializers.Serializer):
    """回退工作流序列化器。"""

    target_node_id = serializers.IntegerField()
    reason = serializers.CharField(required=False, allow_blank=True, default="")
```

- [ ] **Step 2: 编写工作流视图**

Create `backend/apps/workflows/views/workflow_views.py`:

```python
from rest_framework import status, viewsets
from rest_framework.decorators import action
from rest_framework.permissions import IsAuthenticated
from rest_framework.response import Response

from apps.accounts.permissions import MustChangePasswordPermission
from apps.accounts.services import permission_service
from apps.common.exceptions import BadRequest, NotFound, PermissionDenied
from apps.projects.models import Lot
from apps.workflows.models import LotWorkflow, WorkflowNodeInstance
from apps.workflows.serializers.workflow_serializer import (
    LotWorkflowDetailSerializer,
    LotWorkflowSerializer,
    NodeApproveSerializer,
    NodeCompleteSerializer,
    NodeFailSerializer,
    NodeReassignSerializer,
    NodeRejectSerializer,
    NodeSkipSerializer,
    NodeStartSerializer,
    WorkflowNodeInstanceSerializer,
    WorkflowRollbackSerializer,
)
from apps.workflows.services.workflow_service import WorkflowService


class LotWorkflowViewSet(viewsets.ReadOnlyModelViewSet):
    """标段工作流视图集。"""

    permission_classes = [IsAuthenticated, MustChangePasswordPermission]
    serializer_class = LotWorkflowSerializer

    def get_queryset(self):
        """返回用户可访问的工作流。"""
        lot_id = self.kwargs.get("lot_pk")

        # 验证用户有权限访问该标段
        lot = Lot.objects.filter(pk=lot_id).select_related("project").first()
        if not lot:
            return LotWorkflow.objects.none()

        if not permission_service.has_project_permission(
            self.request.user, lot.project, "lot.view"
        ):
            return LotWorkflow.objects.none()

        return LotWorkflow.objects.filter(lot_id=lot_id).prefetch_related("nodes")

    def get_serializer_class(self):
        if self.action == "retrieve":
            return LotWorkflowDetailSerializer
        return LotWorkflowSerializer

    @action(detail=False, methods=["post"])
    def initialize(self, request, lot_pk=None):
        """初始化工作流。"""
        lot = Lot.objects.filter(pk=lot_pk).select_related("project").first()
        if not lot:
            raise NotFound(message="标段不存在")

        if not permission_service.has_project_permission(
            request.user, lot.project, "lot.workflow.operate"
        ):
            raise PermissionDenied(message="无操作工作流权限")

        if lot.has_workflow:
            raise BadRequest(message="标段已存在工作流")

        workflow = WorkflowService.initialize_workflow(lot)

        return Response(
            LotWorkflowSerializer(workflow).data,
            status=status.HTTP_201_CREATED,
        )

    @action(detail=True, methods=["post"])
    def start(self, request, lot_pk=None, pk=None):
        """启动工作流。"""
        workflow = self.get_object()

        if not permission_service.has_project_permission(
            request.user, workflow.lot.project, "lot.workflow.operate"
        ):
            raise PermissionDenied(message="无操作工作流权限")

        workflow = WorkflowService.start_workflow(workflow, request.user)

        return Response(LotWorkflowSerializer(workflow).data)

    @action(detail=True, methods=["post"])
    def rollback(self, request, lot_pk=None, pk=None):
        """回退工作流。"""
        workflow = self.get_object()

        if not permission_service.has_project_permission(
            request.user, workflow.lot.project, "lot.workflow.operate"
        ):
            raise PermissionDenied(message="无操作工作流权限")

        # 只有 owner 可以回退
        from apps.projects.models import ProjectMember
        member = ProjectMember.objects.filter(
            project=workflow.lot.project, user=request.user
        ).first()
        if not member or member.project_role.code != "owner":
            raise PermissionDenied(message="只有项目负责人可以回退流程")

        serializer = WorkflowRollbackSerializer(data=request.data)
        serializer.is_valid(raise_exception=True)

        workflow = WorkflowService.rollback_to_node(
            workflow,
            serializer.validated_data["target_node_id"],
            request.user,
            serializer.validated_data.get("reason", ""),
        )

        return Response(LotWorkflowSerializer(workflow).data)


class WorkflowNodeViewSet(viewsets.ReadOnlyModelViewSet):
    """工作流节点视图集。"""

    permission_classes = [IsAuthenticated, MustChangePasswordPermission]
    serializer_class = WorkflowNodeInstanceSerializer

    def get_queryset(self):
        """返回工作流的节点列表。"""
        workflow_id = self.kwargs.get("workflow_pk")
        return WorkflowNodeInstance.objects.filter(
            lot_workflow_id=workflow_id
        ).order_by("order")

    def get_workflow(self):
        """获取工作流实例。"""
        workflow_id = self.kwargs.get("workflow_pk")
        return LotWorkflow.objects.filter(pk=workflow_id).select_related(
            "lot", "lot__project"
        ).first()

    def check_permission(self, request, workflow):
        """检查权限。"""
        if not permission_service.has_project_permission(
            request.user, workflow.lot.project, "lot.workflow.operate"
        ):
            raise PermissionDenied(message="无操作工作流权限")

    @action(detail=True, methods=["post"])
    def start(self, request, workflow_pk=None, pk=None):
        """开始执行节点。"""
        node = self.get_object()
        workflow = self.get_workflow()

        self.check_permission(request, workflow)

        serializer = NodeStartSerializer(data=request.data)
        serializer.is_valid(raise_exception=True)

        assignee_user_id = serializer.validated_data.get("assignee_user_id")
        assignee_user = None
        if assignee_user_id:
            from apps.accounts.models import User
            assignee_user = User.objects.get(pk=assignee_user_id)

        node = WorkflowService.start_node(node, request.user, assignee_user)

        return Response(WorkflowNodeInstanceSerializer(node).data)

    @action(detail=True, methods=["post"])
    def complete(self, request, workflow_pk=None, pk=None):
        """完成节点。"""
        node = self.get_object()
        workflow = self.get_workflow()

        self.check_permission(request, workflow)

        node = WorkflowService.complete_node(node, request.user)

        return Response(WorkflowNodeInstanceSerializer(node).data)

    @action(detail=True, methods=["post"])
    def fail(self, request, workflow_pk=None, pk=None):
        """标记节点失败。"""
        node = self.get_object()
        workflow = self.get_workflow()

        self.check_permission(request, workflow)

        serializer = NodeFailSerializer(data=request.data)
        serializer.is_valid(raise_exception=True)

        node = WorkflowService.fail_node(
            node, request.user, serializer.validated_data.get("reason", "")
        )

        return Response(WorkflowNodeInstanceSerializer(node).data)

    @action(detail=True, methods=["post"])
    def skip(self, request, workflow_pk=None, pk=None):
        """跳过节点。"""
        node = self.get_object()
        workflow = self.get_workflow()

        self.check_permission(request, workflow)

        # 只有 owner 可以跳过节点
        from apps.projects.models import ProjectMember
        member = ProjectMember.objects.filter(
            project=workflow.lot.project, user=request.user
        ).first()
        if not member or member.project_role.code != "owner":
            raise PermissionDenied(message="只有项目负责人可以跳过节点")

        serializer = NodeSkipSerializer(data=request.data)
        serializer.is_valid(raise_exception=True)

        node = WorkflowService.skip_node(
            node, request.user, serializer.validated_data.get("reason", "")
        )

        return Response(WorkflowNodeInstanceSerializer(node).data)

    @action(detail=True, methods=["post"])
    def approve(self, request, workflow_pk=None, pk=None):
        """审批通过节点。"""
        node = self.get_object()
        workflow = self.get_workflow()

        self.check_permission(request, workflow)

        serializer = NodeApproveSerializer(data=request.data)
        serializer.is_valid(raise_exception=True)

        node = WorkflowService.approve_node(
            node, request.user, serializer.validated_data.get("comment", "")
        )

        return Response(WorkflowNodeInstanceSerializer(node).data)

    @action(detail=True, methods=["post"])
    def reject(self, request, workflow_pk=None, pk=None):
        """审批驳回节点。"""
        node = self.get_object()
        workflow = self.get_workflow()

        self.check_permission(request, workflow)

        serializer = NodeRejectSerializer(data=request.data)
        serializer.is_valid(raise_exception=True)

        node = WorkflowService.reject_node(
            node, request.user, serializer.validated_data["comment"]
        )

        return Response(WorkflowNodeInstanceSerializer(node).data)

    @action(detail=True, methods=["post"])
    def reassign(self, request, workflow_pk=None, pk=None):
        """重新指派节点负责人。"""
        node = self.get_object()
        workflow = self.get_workflow()

        self.check_permission(request, workflow)

        serializer = NodeReassignSerializer(data=request.data)
        serializer.is_valid(raise_exception=True)

        node.assignee_type = serializer.validated_data["assignee_type"]
        node.assignee_role = serializer.validated_data.get("assignee_role", "")
        assignee_user_id = serializer.validated_data.get("assignee_user_id")
        if assignee_user_id:
            from apps.accounts.models import User
            node.assignee_user_id = assignee_user_id
        else:
            node.assignee_user = None

        node.save()

        return Response(WorkflowNodeInstanceSerializer(node).data)
```

- [ ] **Step 3: 更新 URL 路由**

Read `backend/apps/workflows/urls.py` and add:

```python
from django.urls import include, path
from rest_framework.routers import DefaultRouter

from apps.workflows.views.template_views import (
    ProjectTemplateViewSet,
    SystemTemplateNodeViewSet,
    SystemTemplateViewSet,
)
from apps.workflows.views.workflow_views import (
    LotWorkflowViewSet,
    WorkflowNodeViewSet,
)

router = DefaultRouter()
router.register(r"workflow-templates", SystemTemplateViewSet, basename="workflow-template")

urlpatterns = [
    path("", include(router.urls)),
    # 系统模板节点管理
    path(
        "workflow-templates/<int:template_pk>/nodes/",
        SystemTemplateNodeViewSet.as_view({"get": "list", "post": "create"}),
        name="workflow-template-nodes",
    ),
    path(
        "workflow-templates/<int:template_pk>/nodes/<int:pk>/",
        SystemTemplateNodeViewSet.as_view({
            "get": "retrieve",
            "put": "update",
            "patch": "partial_update",
            "delete": "destroy",
        }),
        name="workflow-template-node-detail",
    ),
    path(
        "workflow-templates/<int:template_pk>/nodes/reorder/",
        SystemTemplateNodeViewSet.as_view({"post": "reorder"}),
        name="workflow-template-nodes-reorder",
    ),
    # 项目模板查看
    path(
        "projects/<int:project_pk>/workflow-templates/",
        ProjectTemplateViewSet.as_view({"get": "list"}),
        name="project-workflow-templates",
    ),
    path(
        "projects/<int:project_pk>/workflow-templates/<int:pk>/",
        ProjectTemplateViewSet.as_view({"get": "retrieve"}),
        name="project-workflow-template-detail",
    ),
    # 标段工作流
    path(
        "lots/<int:lot_pk>/workflow/",
        LotWorkflowViewSet.as_view({
            "get": "list",
            "post": "initialize",
        }),
        name="lot-workflow",
    ),
    path(
        "lots/<int:lot_pk>/workflow/<int:pk>/",
        LotWorkflowViewSet.as_view({"get": "retrieve"}),
        name="lot-workflow-detail",
    ),
    path(
        "lots/<int:lot_pk>/workflow/<int:pk>/start/",
        LotWorkflowViewSet.as_view({"post": "start"}),
        name="lot-workflow-start",
    ),
    path(
        "lots/<int:lot_pk>/workflow/<int:pk>/rollback/",
        LotWorkflowViewSet.as_view({"post": "rollback"}),
        name="lot-workflow-rollback",
    ),
    # 工作流节点
    path(
        "lots/<int:lot_pk>/workflow/<int:workflow_pk>/nodes/",
        WorkflowNodeViewSet.as_view({"get": "list"}),
        name="workflow-nodes",
    ),
    path(
        "lots/<int:lot_pk>/workflow/<int:workflow_pk>/nodes/<int:pk>/",
        WorkflowNodeViewSet.as_view({"get": "retrieve"}),
        name="workflow-node-detail",
    ),
    path(
        "lots/<int:lot_pk>/workflow/<int:workflow_pk>/nodes/<int:pk>/start/",
        WorkflowNodeViewSet.as_view({"post": "start"}),
        name="workflow-node-start",
    ),
    path(
        "lots/<int:lot_pk>/workflow/<int:workflow_pk>/nodes/<int:pk>/complete/",
        WorkflowNodeViewSet.as_view({"post": "complete"}),
        name="workflow-node-complete",
    ),
    path(
        "lots/<int:lot_pk>/workflow/<int:workflow_pk>/nodes/<int:pk>/fail/",
        WorkflowNodeViewSet.as_view({"post": "fail"}),
        name="workflow-node-fail",
    ),
    path(
        "lots/<int:lot_pk>/workflow/<int:workflow_pk>/nodes/<int:pk>/skip/",
        WorkflowNodeViewSet.as_view({"post": "skip"}),
        name="workflow-node-skip",
    ),
    path(
        "lots/<int:lot_pk>/workflow/<int:workflow_pk>/nodes/<int:pk>/approve/",
        WorkflowNodeViewSet.as_view({"post": "approve"}),
        name="workflow-node-approve",
    ),
    path(
        "lots/<int:lot_pk>/workflow/<int:workflow_pk>/nodes/<int:pk>/reject/",
        WorkflowNodeViewSet.as_view({"post": "reject"}),
        name="workflow-node-reject",
    ),
    path(
        "lots/<int:lot_pk>/workflow/<int:workflow_pk>/nodes/<int:pk>/reassign/",
        WorkflowNodeViewSet.as_view({"post": "reassign"}),
        name="workflow-node-reassign",
    ),
]
```

- [ ] **Step 4: 编写 API 测试**

Create `backend/apps/workflows/tests/test_workflow_api.py`:

```python
import pytest
from rest_framework.test import APIClient

from apps.workflows.models import LotWorkflow, WorkflowNodeInstance
from apps.workflows.services.workflow_service import WorkflowService
from apps.projects.models import ProjectMember


@pytest.fixture
def api_client():
    return APIClient()


@pytest.fixture
def setup_lot_with_permission(api_client, user_factory, lot_factory, project_factory):
    """创建有权限的用户和标段。"""
    user = user_factory()
    project = project_factory()
    lot = lot_factory(project=project)

    # 初始化角色并添加用户为 owner
    from apps.projects.services.role_service import RoleService
    roles = RoleService.initialize_builtin_roles(project)
    owner_role = next(r for r in roles if r.code == "owner")
    ProjectMember.objects.create(project=project, user=user, project_role=owner_role)

    api_client.force_authenticate(user=user)
    return api_client, user, lot, project


@pytest.mark.django_db
def test_initialize_workflow(setup_lot_with_permission, workflow_template_factory):
    """测试初始化工作流 API。"""
    client, user, lot, project = setup_lot_with_permission

    # 创建项目模板
    template = workflow_template_factory(scope="project", project=project)
    from apps.workflows.models import WorkflowNodeTemplate
    WorkflowNodeTemplate.objects.create(
        workflow_template=template, name="节点1", order=1
    )

    response = client.post(f"/api/lots/{lot.id}/workflow/")

    assert response.status_code == 201
    assert response.data["status"] == "not_started"
    assert len(response.data["nodes"]) == 1


@pytest.mark.django_db
def test_start_workflow(setup_lot_with_permission, workflow_template_factory):
    """测试启动工作流 API。"""
    client, user, lot, project = setup_lot_with_permission

    template = workflow_template_factory(scope="project", project=project)
    from apps.workflows.models import WorkflowNodeTemplate
    WorkflowNodeTemplate.objects.create(
        workflow_template=template, name="节点1", order=1
    )

    workflow = WorkflowService.initialize_workflow(lot, template)

    response = client.post(f"/api/lots/{lot.id}/workflow/{workflow.id}/start/")

    assert response.status_code == 200
    assert response.data["status"] == "in_progress"


@pytest.mark.django_db
def test_complete_node(setup_lot_with_permission, workflow_template_factory):
    """测试完成节点 API。"""
    client, user, lot, project = setup_lot_with_permission

    template = workflow_template_factory(scope="project", project=project)
    from apps.workflows.models import WorkflowNodeTemplate
    WorkflowNodeTemplate.objects.create(
        workflow_template=template, name="节点1", order=1
    )

    workflow = WorkflowService.initialize_workflow(lot, template)
    WorkflowService.start_workflow(workflow, user)

    node = workflow.nodes.first()

    response = client.post(
        f"/api/lots/{lot.id}/workflow/{workflow.id}/nodes/{node.id}/complete/"
    )

    assert response.status_code == 200
    assert response.data["status"] == "completed"


@pytest.mark.django_db
def test_approve_node(setup_lot_with_permission, workflow_template_factory):
    """测试审批节点 API。"""
    client, user, lot, project = setup_lot_with_permission

    template = workflow_template_factory(scope="project", project=project)
    from apps.workflows.models import WorkflowNodeTemplate
    WorkflowNodeTemplate.objects.create(
        workflow_template=template, name="节点1", order=1,
        requires_approval=True, approver_role="owner"
    )

    workflow = WorkflowService.initialize_workflow(lot, template)
    WorkflowService.start_workflow(workflow, user)

    node = workflow.nodes.first()
    node.status = "in_progress"
    node.started_at = timezone.now()
    node.save()

    response = client.post(
        f"/api/lots/{lot.id}/workflow/{workflow.id}/nodes/{node.id}/approve/",
        {"comment": "审批通过"}
    )

    assert response.status_code == 200
    assert response.data["approval_status"] == "approved"


@pytest.mark.django_db
def test_skip_node_owner_only(setup_lot_with_permission, workflow_template_factory):
    """测试跳过节点（仅 owner）。"""
    client, user, lot, project = setup_lot_with_permission

    template = workflow_template_factory(scope="project", project=project)
    from apps.workflows.models import WorkflowNodeTemplate
    WorkflowNodeTemplate.objects.create(
        workflow_template=template, name="节点1", order=1
    )

    workflow = WorkflowService.initialize_workflow(lot, template)
    WorkflowService.start_workflow(workflow, user)

    node = workflow.nodes.first()

    response = client.post(
        f"/api/lots/{lot.id}/workflow/{workflow.id}/nodes/{node.id}/skip/",
        {"reason": "跳过测试"}
    )

    # 用户是 owner，应该成功
    assert response.status_code == 200
    assert response.data["status"] == "skipped"


@pytest.mark.django_db
def test_skip_node_non_owner_fails(api_client, user_factory, lot_factory, project_factory, workflow_template_factory):
    """测试非 owner 跳过节点失败。"""
    user = user_factory()
    project = project_factory()
    lot = lot_factory(project=project)

    # 初始化角色并添加用户为 editor
    from apps.projects.services.role_service import RoleService
    roles = RoleService.initialize_builtin_roles(project)
    editor_role = next(r for r in roles if r.code == "editor")
    ProjectMember.objects.create(project=project, user=user, project_role=editor_role)

    api_client.force_authenticate(user=user)

    template = workflow_template_factory(scope="project", project=project)
    from apps.workflows.models import WorkflowNodeTemplate
    WorkflowNodeTemplate.objects.create(
        workflow_template=template, name="节点1", order=1
    )

    # 创建 owner 并初始化工作流
    owner = user_factory()
    owner_role = next(r for r in roles if r.code == "owner")
    ProjectMember.objects.create(project=project, user=owner, project_role=owner_role)

    workflow = WorkflowService.initialize_workflow(lot, template)
    WorkflowService.start_workflow(workflow, owner)

    node = workflow.nodes.first()

    response = api_client.post(
        f"/api/lots/{lot.id}/workflow/{workflow.id}/nodes/{node.id}/skip/",
        {"reason": "跳过测试"}
    )

    assert response.status_code == 403
```

- [ ] **Step 5: 运行测试**

```bash
cd backend && pytest apps/workflows/tests/test_workflow_api.py -v
```

Expected: PASS

- [ ] **Step 6: 提交**

```bash
git add backend/apps/workflows/
git commit -m "feat(workflows): 新增工作流执行 API

- LotWorkflowViewSet: 工作流初始化、启动、回退
- WorkflowNodeViewSet: 节点执行、完成、失败、跳过、审批
- 权限控制：lot.workflow.operate
- 审批自我回避检查
- 回退/跳过仅 owner 可操作

Co-Authored-By: Claude Opus 4.7 <noreply@anthropic.com>"
```

---

## Self-Review

### 1. Spec Coverage Check

| 需求 | 任务 |
|------|------|
| LotWorkflow 模型 | Task 1 |
| WorkflowNodeInstance 模型 | Task 2 |
| Lot 添加 workflow_status | Task 2 |
| 工作流执行服务 | Task 3 |
| 节点操作 API | Task 4 |
| 审批自我回避 | Task 3, Task 4 |
| 自动推进规则 | Task 3 |
| 回退规则 | Task 3, Task 4 |
| 并发控制 | Task 3 |

### 2. Placeholder Scan

- ✅ 无 "TBD"、"TODO"、"implement later"
- ✅ 所有代码步骤都有完整代码
- ✅ 所有命令都有预期输出

### 3. Type Consistency

- ✅ WorkflowNodeInstance.status 使用 "pending" / "in_progress" 等字符串
- ✅ approval_status 使用 "not_required" / "pending" / "approved" / "rejected"
- ✅ 序列化器字段名与模型字段一致

---

## Execution Handoff

**Plan complete and saved to `docs/superpowers/plans/2026-05-22-phase4.3-lot-workflow.md`. Two execution options:**

**1. Subagent-Driven (recommended)** - 我为每个任务派发独立子代理，任务间审查，快速迭代

**2. Inline Execution** - 在当前会话中使用 executing-plans 执行，批量执行带检查点

**选择哪种方式？**
