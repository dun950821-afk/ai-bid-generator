# Phase 4.1: 项目基础 + 自定义角色 实施计划

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 实现自定义项目角色模型、权限缓存机制、成员管理 API，支撑项目级权限灵活配置。

**Architecture:** 新增 ProjectRole 模型存储项目级角色，修改 ProjectMember 关联 FK；权限服务改造支持动态角色权限校验；Redis 缓存提升鉴权性能。

**Tech Stack:** Django 5 / DRF / Redis / pytest

---

## File Structure

### 新建文件
- `backend/apps/projects/models/__init__.py` — 模型包初始化
- `backend/apps/projects/models/project.py` — Project 模型（从原 models.py 拆分）
- `backend/apps/projects/models/lot.py` — Lot 模型
- `backend/apps/projects/models/project_member.py` — ProjectMember 模型
- `backend/apps/projects/models/project_role.py` — ProjectRole 模型（新建）
- `backend/apps/projects/services/__init__.py` — 服务包初始化
- `backend/apps/projects/services/role_service.py` — 角色服务（创建/初始化/校验）
- `backend/apps/projects/services/project_service.py` — 项目服务（创建项目+初始化角色）
- `backend/apps/projects/serializers/__init__.py` — 序列化器包
- `backend/apps/projects/serializers/project_serializer.py` — 项目序列化器
- `backend/apps/projects/serializers/role_serializer.py` — 角色序列化器
- `backend/apps/projects/serializers/member_serializer.py` — 成员序列化器
- `backend/apps/projects/views/__init__.py` — 视图包
- `backend/apps/projects/views/project_views.py` — 项目视图
- `backend/apps/projects/views/role_views.py` — 角色视图
- `backend/apps/projects/views/member_views.py` — 成员视图
- `backend/apps/projects/tests/test_project_role.py` — 角色模型测试
- `backend/apps/projects/tests/test_role_service.py` — 角色服务测试
- `backend/apps/projects/tests/test_member_api.py` — 成员 API 测试

### 修改文件
- `backend/apps/projects/models.py` — 拆分后保留导入或删除
- `backend/apps/accounts/services/permission_service.py` — 扩展支持动态角色权限
- `backend/apps/accounts/permissions_registry.py` — 新增项目管理权限码
- `backend/apps/projects/urls.py` — 新增路由
- `backend/apps/projects/signals.py` — 项目创建时初始化角色

---

## Task 1: 创建 ProjectRole 模型

**Files:**
- Create: `backend/apps/projects/models/__init__.py`
- Create: `backend/apps/projects/models/project_role.py`
- Create: `backend/apps/projects/tests/test_project_role.py`

- [ ] **Step 1: 创建模型包结构**

```bash
mkdir -p backend/apps/projects/models
touch backend/apps/projects/models/__init__.py
```

- [ ] **Step 2: 编写 ProjectRole 模型**

Create `backend/apps/projects/models/project_role.py`:

```python
from django.conf import settings
from django.db import models

from apps.common.models import TimeStampedModel


class ProjectRole(TimeStampedModel):
    """项目角色（支持自定义）。"""

    CORE_OWNER_PERMISSIONS = [
        "project.view",
        "project.update",
        "project.member.manage",
    ]

    project = models.ForeignKey(
        "projects.Project",
        on_delete=models.CASCADE,
        related_name="roles",
        verbose_name="所属项目",
    )
    name = models.CharField("角色名称", max_length=128)
    code = models.CharField("角色编码", max_length=64)
    permissions = models.JSONField("权限码列表", default=list)
    is_builtin = models.BooleanField("是否内置角色", default=False)
    is_default = models.BooleanField("是否默认角色", default=False)
    created_by = models.ForeignKey(
        settings.AUTH_USER_MODEL,
        on_delete=models.SET_NULL,
        null=True,
        blank=True,
        related_name="created_project_roles",
        verbose_name="创建人",
    )

    class Meta:
        db_table = "projects_project_role"
        constraints = [
            models.UniqueConstraint(
                fields=["project", "code"],
                name="uniq_projectrole_project_code",
            )
        ]
        indexes = [
            models.Index(fields=["project"]),
        ]

    def __str__(self):
        return f"{self.project.name} / {self.name}"

    def save(self, *args, **kwargs):
        """Owner 角色自动合并核心权限。"""
        if self.code == "owner":
            merged = set(self.permissions) | set(self.CORE_OWNER_PERMISSIONS)
            self.permissions = list(merged)
        super().save(*args, **kwargs)
```

- [ ] **Step 3: 编写模型测试**

Create `backend/apps/projects/tests/test_project_role.py`:

```python
import pytest
from apps.projects.models.project_role import ProjectRole
from apps.projects.models import Project


@pytest.mark.django_db
def test_project_role_creation(project_factory, user_factory):
    """测试角色创建。"""
    project = project_factory()
    user = user_factory()
    role = ProjectRole.objects.create(
        project=project,
        name="自定义角色",
        code="custom_role",
        permissions=["project.view", "lot.view"],
        created_by=user,
    )
    assert role.id is not None
    assert role.permissions == ["project.view", "lot.view"]


@pytest.mark.django_db
def test_owner_role_auto_merge_core_permissions(project_factory, user_factory):
    """测试 owner 角色自动合并核心权限。"""
    project = project_factory()
    user = user_factory()
    role = ProjectRole.objects.create(
        project=project,
        name="负责人",
        code="owner",
        permissions=["project.view"],  # 故意缺少核心权限
        is_builtin=True,
        created_by=user,
    )
    # 保存后应自动合并核心权限
    assert "project.update" in role.permissions
    assert "project.member.manage" in role.permissions


@pytest.mark.django_db
def test_unique_constraint(project_factory):
    """测试项目内 code 唯一约束。"""
    project = project_factory()
    ProjectRole.objects.create(
        project=project,
        name="角色A",
        code="role_a",
        permissions=[],
    )
    with pytest.raises(Exception):  # IntegrityError
        ProjectRole.objects.create(
            project=project,
            name="角色B",
            code="role_a",  # 重复 code
            permissions=[],
        )
```

- [ ] **Step 4: 运行测试验证模型**

```bash
cd backend && pytest apps/projects/tests/test_project_role.py -v
```

Expected: 测试应失败（需要迁移文件）

---

## Task 2: 拆分现有模型并修改 ProjectMember

**Files:**
- Create: `backend/apps/projects/models/project.py`
- Create: `backend/apps/projects/models/lot.py`
- Create: `backend/apps/projects/models/project_member.py`
- Modify: `backend/apps/projects/models/__init__.py`
- Delete: `backend/apps/projects/models.py`（原文件）

- [ ] **Step 1: 创建 Project 模型文件**

Create `backend/apps/projects/models/project.py`:

```python
from django.conf import settings
from django.db import models

from apps.common.models import TimeStampedModel


class Project(TimeStampedModel):
    """项目。"""

    STATUS_CHOICES = [
        ("active", "进行中"),
        ("archived", "已归档"),
        ("closed", "已关闭"),
    ]

    name = models.CharField("项目名", max_length=255)
    description = models.TextField("描述", blank=True, default="")
    status = models.CharField(
        "状态", max_length=32, choices=STATUS_CHOICES, default="active"
    )
    created_by = models.ForeignKey(
        settings.AUTH_USER_MODEL,
        on_delete=models.PROTECT,
        related_name="created_projects",
        verbose_name="创建人",
    )

    class Meta:
        db_table = "projects_project"
        ordering = ["-created_at"]

    def __str__(self):
        return self.name
```

- [ ] **Step 2: 创建 Lot 模型文件**

Create `backend/apps/projects/models/lot.py`:

```python
from django.db import models

from apps.common.models import TimeStampedModel


class Lot(TimeStampedModel):
    """标段。"""

    STATUS_CHOICES = [
        ("active", "进行中"),
        ("archived", "已归档"),
    ]

    project = models.ForeignKey(
        "projects.Project", on_delete=models.CASCADE, related_name="lots", verbose_name="项目"
    )
    name = models.CharField("标段名称", max_length=255)
    code = models.CharField("标段编号", max_length=64, blank=True)
    status = models.CharField(
        "状态", max_length=32, choices=STATUS_CHOICES, default="active"
    )

    class Meta:
        db_table = "projects_lot"
        ordering = ["id"]
        indexes = [models.Index(fields=["project"])]

    def __str__(self):
        return f"{self.project.name} / {self.name}"
```

- [ ] **Step 3: 创建 ProjectMember 模型文件（修改 project_role 为 FK）**

Create `backend/apps/projects/models/project_member.py`:

```python
from django.conf import settings
from django.db import models

from apps.common.models import TimeStampedModel


class ProjectMember(TimeStampedModel):
    """项目成员（一个用户在一个项目内只有一个角色）。"""

    project = models.ForeignKey(
        "projects.Project", on_delete=models.CASCADE, related_name="members", verbose_name="项目"
    )
    user = models.ForeignKey(
        settings.AUTH_USER_MODEL,
        on_delete=models.CASCADE,
        related_name="project_memberships",
        verbose_name="用户",
    )
    project_role = models.ForeignKey(
        "projects.ProjectRole",
        on_delete=models.PROTECT,
        related_name="members",
        verbose_name="项目角色",
    )
    added_by = models.ForeignKey(
        settings.AUTH_USER_MODEL,
        on_delete=models.SET_NULL,
        null=True,
        blank=True,
        related_name="added_members",
        verbose_name="添加人",
    )

    class Meta:
        db_table = "projects_project_member"
        constraints = [
            models.UniqueConstraint(
                fields=["project", "user"], name="uniq_projectmember_project_user"
            )
        ]
        indexes = [
            models.Index(fields=["project"]),
            models.Index(fields=["user"]),
        ]

    def __str__(self):
        return f"{self.project.name} / {self.user.username} ({self.project_role.name})"
```

- [ ] **Step 4: 更新模型包 `__init__.py`**

Edit `backend/apps/projects/models/__init__.py`:

```python
from .project import Project
from .lot import Lot
from .project_member import ProjectMember
from .project_role import ProjectRole

__all__ = ["Project", "Lot", "ProjectMember", "ProjectRole"]
```

- [ ] **Step 5: 删除原 models.py（或改为导入）**

```bash
# 备份原文件后删除
mv backend/apps/projects/models.py backend/apps/projects/models.py.bak
```

- [ ] **Step 6: 创建迁移文件**

```bash
cd backend && python manage.py makemigrations projects --name add_project_role
```

- [ ] **Step 7: 编写数据迁移脚本（迁移现有 project_role 字段）**

Create `backend/apps/projects/migrations/0002_migrate_project_role.py`:

```python
from django.db import migrations


def migrate_project_role(apps, schema_editor):
    """将现有的 project_role 字符串迁移到 FK。"""
    ProjectMember = apps.get_model("projects", "ProjectMember")
    ProjectRole = apps.get_model("projects", "ProjectRole")
    Project = apps.get_model("projects", "Project")
    User = apps.get_model("accounts", "User")

    # 获取所有项目
    for project in Project.objects.all():
        # 为每个项目创建内置角色（如果不存在）
        for code, name in [("owner", "负责人"), ("editor", "编辑"), ("reviewer", "评审"), ("viewer", "只读")]:
            ProjectRole.objects.get_or_create(
                project=project,
                code=code,
                defaults={"name": name, "is_builtin": True, "permissions": []}
            )

    # 迁移成员的角色关联
    role_mapping = {
        "owner": "owner",
        "editor": "editor",
        "reviewer": "reviewer",
        "viewer": "viewer",
    }

    for member in ProjectMember.objects.all():
        old_role = member.project_role  # 此时还是字符串
        if old_role in role_mapping:
            role = ProjectRole.objects.get(
                project_id=member.project_id,
                code=role_mapping[old_role]
            )
            # 使用原始 SQL 更新，因为字段类型已改变
            from django.db import connection
            with connection.cursor() as cursor:
                cursor.execute(
                    "UPDATE projects_project_member SET project_role_id = %s WHERE id = %s",
                    [role.id, member.id]
                )


class Migration(migrations.Migration):
    dependencies = [
        ("projects", "0001_initial"),
        ("projects", "0002_add_project_role"),
    ]

    operations = [
        migrations.RunPython(migrate_project_role, migrations.RunPython.noop),
    ]
```

- [ ] **Step 8: 运行迁移**

```bash
cd backend && python manage.py migrate projects
```

- [ ] **Step 9: 运行测试验证**

```bash
cd backend && pytest apps/projects/tests/ -v
```

---

## Task 3: 创建角色服务

**Files:**
- Create: `backend/apps/projects/services/__init__.py`
- Create: `backend/apps/projects/services/role_service.py`
- Create: `backend/apps/projects/tests/test_role_service.py`

- [ ] **Step 1: 创建服务包**

```bash
mkdir -p backend/apps/projects/services
touch backend/apps/projects/services/__init__.py
```

- [ ] **Step 2: 编写角色服务**

Create `backend/apps/projects/services/role_service.py`:

```python
"""项目角色服务。"""
from django.db import transaction

from apps.projects.models import Project, ProjectRole


# 内置角色配置
BUILTIN_ROLES = [
    {
        "code": "owner",
        "name": "负责人",
        "permissions": [
            "project.view", "project.update", "project.delete",
            "project.member.manage", "project.role.manage",
            "lot.create", "lot.view", "lot.update", "lot.workflow.operate",
            "tender.view", "tender.upload", "tender.parse",
            "outline.view", "outline.edit",
            "section.view", "section.generate", "section.edit", "section.review",
            "export.create", "export.view",
        ],
        "is_builtin": True,
    },
    {
        "code": "editor",
        "name": "编辑",
        "permissions": [
            "project.view",
            "lot.view", "lot.workflow.operate",
            "tender.view", "tender.upload",
            "outline.view", "outline.edit",
            "section.view", "section.generate", "section.edit",
        ],
        "is_builtin": True,
    },
    {
        "code": "reviewer",
        "name": "评审",
        "permissions": [
            "project.view",
            "lot.view",
            "tender.view",
            "outline.view",
            "section.view", "section.review",
        ],
        "is_builtin": True,
    },
    {
        "code": "viewer",
        "name": "只读",
        "permissions": [
            "project.view",
            "lot.view",
            "tender.view",
            "outline.view",
            "section.view",
            "export.view",
        ],
        "is_builtin": True,
    },
]


class RoleService:
    """项目角色服务。"""

    @staticmethod
    @transaction.atomic
    def initialize_builtin_roles(project: Project, created_by=None) -> list[ProjectRole]:
        """初始化项目的内置角色。

        Args:
            project: 项目实例
            created_by: 创建人

        Returns:
            创建的角色列表
        """
        roles = []
        for role_config in BUILTIN_ROLES:
            role, _ = ProjectRole.objects.get_or_create(
                project=project,
                code=role_config["code"],
                defaults={
                    "name": role_config["name"],
                    "permissions": role_config["permissions"],
                    "is_builtin": role_config["is_builtin"],
                    "created_by": created_by,
                }
            )
            roles.append(role)
        return roles

    @staticmethod
    def get_role_by_code(project: Project, code: str) -> ProjectRole | None:
        """根据 code 获取项目角色。

        Args:
            project: 项目实例
            code: 角色编码

        Returns:
            角色实例或 None
        """
        try:
            return ProjectRole.objects.get(project=project, code=code)
        except ProjectRole.DoesNotExist:
            return None

    @staticmethod
    @transaction.atomic
    def update_role_permissions(role: ProjectRole, permissions: list[str], updated_by=None) -> ProjectRole:
        """更新角色权限。

        Args:
            role: 角色实例
            permissions: 新权限列表
            updated_by: 更新人

        Returns:
            更新后的角色
        """
        role.permissions = permissions
        role.save()
        return role

    @staticmethod
    def can_delete_role(role: ProjectRole) -> bool:
        """检查角色是否可删除。

        Args:
            role: 角色实例

        Returns:
            是否可删除
        """
        # 内置角色不可删除
        if role.is_builtin:
            return False
        # 有成员关联的角色不可删除
        if role.members.exists():
            return False
        return True
```

- [ ] **Step 3: 编写角色服务测试**

Create `backend/apps/projects/tests/test_role_service.py`:

```python
import pytest
from apps.projects.services.role_service import RoleService, BUILTIN_ROLES
from apps.projects.models import Project, ProjectRole, ProjectMember


@pytest.mark.django_db
def test_initialize_builtin_roles(project_factory, user_factory):
    """测试初始化内置角色。"""
    project = project_factory()
    user = user_factory()

    roles = RoleService.initialize_builtin_roles(project, created_by=user)

    assert len(roles) == 4
    role_codes = [r.code for r in roles]
    assert "owner" in role_codes
    assert "editor" in role_codes
    assert "reviewer" in role_codes
    assert "viewer" in role_codes

    # 验证角色都有 is_builtin=True
    for role in roles:
        assert role.is_builtin is True


@pytest.mark.django_db
def test_get_role_by_code(project_factory):
    """测试根据 code 获取角色。"""
    project = project_factory()
    RoleService.initialize_builtin_roles(project)

    role = RoleService.get_role_by_code(project, "owner")
    assert role is not None
    assert role.code == "owner"

    # 不存在的 code
    role = RoleService.get_role_by_code(project, "nonexistent")
    assert role is None


@pytest.mark.django_db
def test_update_role_permissions(project_factory, user_factory):
    """测试更新角色权限。"""
    project = project_factory()
    user = user_factory()
    RoleService.initialize_builtin_roles(project)

    role = RoleService.get_role_by_code(project, "editor")
    new_permissions = ["project.view", "lot.view", "custom.permission"]

    updated = RoleService.update_role_permissions(role, new_permissions)

    assert "custom.permission" in updated.permissions


@pytest.mark.django_db
def test_owner_role_auto_merge_core_permissions(project_factory, user_factory):
    """测试更新 owner 角色时自动合并核心权限。"""
    project = project_factory()
    user = user_factory()
    RoleService.initialize_builtin_roles(project)

    role = RoleService.get_role_by_code(project, "owner")
    # 尝试移除核心权限
    new_permissions = ["project.view", "lot.view"]

    updated = RoleService.update_role_permissions(role, new_permissions)

    # 核心权限应被自动保留
    assert "project.update" in updated.permissions
    assert "project.member.manage" in updated.permissions


@pytest.mark.django_db
def test_can_delete_role(project_factory, user_factory):
    """测试角色删除检查。"""
    project = project_factory()
    user = user_factory()
    RoleService.initialize_builtin_roles(project)

    # 内置角色不可删除
    owner_role = RoleService.get_role_by_code(project, "owner")
    assert RoleService.can_delete_role(owner_role) is False

    # 创建自定义角色
    custom_role = ProjectRole.objects.create(
        project=project,
        name="自定义角色",
        code="custom",
        permissions=["project.view"],
        created_by=user,
    )
    assert RoleService.can_delete_role(custom_role) is True

    # 关联成员后不可删除
    ProjectMember.objects.create(
        project=project,
        user=user,
        project_role=custom_role,
    )
    assert RoleService.can_delete_role(custom_role) is False
```

- [ ] **Step 4: 运行测试**

```bash
cd backend && pytest apps/projects/tests/test_role_service.py -v
```

Expected: PASS

- [ ] **Step 5: 提交**

```bash
git add backend/apps/projects/models/ backend/apps/projects/services/ backend/apps/projects/tests/test_project_role.py backend/apps/projects/tests/test_role_service.py
git commit -m "feat(projects): 新增 ProjectRole 模型和角色服务

- ProjectRole 支持自定义项目角色
- Owner 角色核心权限自动锁定
- RoleService 提供初始化、查询、更新功能

Co-Authored-By: Claude Opus 4.7 <noreply@anthropic.com>"
```

---

## Task 4: 扩展权限服务支持动态角色

**Files:**
- Modify: `backend/apps/accounts/services/permission_service.py`
- Modify: `backend/apps/accounts/permissions_registry.py`
- Create: `backend/apps/accounts/tests/test_project_permission.py`

- [ ] **Step 1: 读取现有权限服务**

```bash
cat backend/apps/accounts/services/permission_service.py
```

- [ ] **Step 2: 扩展权限服务**

Modify `backend/apps/accounts/services/permission_service.py`, 添加以下方法:

```python
from django.core.cache import cache


def has_project_permission(user, project, code):
    """检查用户在项目的权限（带缓存）。

    Args:
        user: 用户实例
        project: 项目实例
        code: 权限码

    Returns:
        是否有权限
    """
    cache_key = f"project_perms:{project.id}:{user.id}"
    cached = cache.get(cache_key)

    if cached is not None:
        return code in cached

    # 查询用户的 ProjectMember 记录
    from apps.projects.models import ProjectMember
    try:
        member = ProjectMember.objects.select_related("project_role").get(
            project=project, user=user
        )
        permissions = set(member.project_role.permissions)
    except ProjectMember.DoesNotExist:
        permissions = set()

    cache.set(cache_key, permissions, timeout=300)
    return code in permissions


def invalidate_user_project_permission_cache(project_id, user_id):
    """驱逐单个用户的项目权限缓存。"""
    cache_key = f"project_perms:{project_id}:{user_id}"
    cache.delete(cache_key)


def invalidate_project_role_cache(project_id, role_id):
    """驱逐角色关联的所有用户缓存。"""
    from apps.projects.models import ProjectMember
    user_ids = ProjectMember.objects.filter(
        project_id=project_id,
        project_role_id=role_id
    ).values_list("user_id", flat=True)
    cache.delete_many([
        f"project_perms:{project_id}:{uid}" for uid in user_ids
    ])
```

- [ ] **Step 3: 新增权限码注册**

Modify `backend/apps/accounts/permissions_registry.py`, 添加:

```python
# 项目管理权限
PROJECT_PERMISSIONS = [
    {"code": "project.create", "name": "创建项目", "module": "projects", "scope": "global"},
    {"code": "project.view", "name": "查看项目", "module": "projects", "scope": "project"},
    {"code": "project.update", "name": "更新项目", "module": "projects", "scope": "project"},
    {"code": "project.delete", "name": "删除项目", "module": "projects", "scope": "project"},
    {"code": "project.member.manage", "name": "管理成员", "module": "projects", "scope": "project"},
    {"code": "project.role.manage", "name": "管理角色", "module": "projects", "scope": "project"},
    # 标段权限
    {"code": "lot.create", "name": "创建标段", "module": "projects", "scope": "project"},
    {"code": "lot.view", "name": "查看标段", "module": "projects", "scope": "project"},
    {"code": "lot.update", "name": "更新标段", "module": "projects", "scope": "project"},
    {"code": "lot.workflow.operate", "name": "操作工作流", "module": "projects", "scope": "project"},
]

PERMISSIONS.extend(PROJECT_PERMISSIONS)
```

- [ ] **Step 4: 编写权限服务测试**

Create `backend/apps/accounts/tests/test_project_permission.py`:

```python
import pytest
from django.core.cache import cache
from apps.accounts.services import permission_service
from apps.projects.models import Project, ProjectRole, ProjectMember


@pytest.fixture(autouse=True)
def clear_cache():
    cache.clear()
    yield
    cache.clear()


@pytest.mark.django_db
def test_has_project_permission_with_cache(project_factory, user_factory):
    """测试项目权限检查（带缓存）。"""
    project = project_factory()
    user = user_factory()

    # 初始化角色并添加成员
    from apps.projects.services.role_service import RoleService
    roles = RoleService.initialize_builtin_roles(project)
    editor_role = next(r for r in roles if r.code == "editor")

    ProjectMember.objects.create(
        project=project,
        user=user,
        project_role=editor_role,
    )

    # 检查权限
    assert permission_service.has_project_permission(user, project, "project.view") is True
    assert permission_service.has_project_permission(user, project, "project.update") is False


@pytest.mark.django_db
def test_permission_cache_invalidation(project_factory, user_factory):
    """测试权限缓存失效。"""
    project = project_factory()
    user = user_factory()

    from apps.projects.services.role_service import RoleService
    roles = RoleService.initialize_builtin_roles(project)
    editor_role = next(r for r in roles if r.code == "editor")
    owner_role = next(r for r in roles if r.code == "owner")

    member = ProjectMember.objects.create(
        project=project,
        user=user,
        project_role=editor_role,
    )

    # 首次查询，写入缓存
    permission_service.has_project_permission(user, project, "project.view")

    # 修改角色
    member.project_role = owner_role
    member.save()

    # 失效缓存
    permission_service.invalidate_user_project_permission_cache(project.id, user.id)

    # 再次查询应使用新权限
    assert permission_service.has_project_permission(user, project, "project.update") is True


@pytest.mark.django_db
def test_invalidate_project_role_cache(project_factory, user_factory):
    """测试批量失效角色关联用户缓存。"""
    project = project_factory()
    user1 = user_factory()
    user2 = user_factory()

    from apps.projects.services.role_service import RoleService
    roles = RoleService.initialize_builtin_roles(project)
    editor_role = next(r for r in roles if r.code == "editor")

    # 两个用户都是 editor
    ProjectMember.objects.create(project=project, user=user1, project_role=editor_role)
    ProjectMember.objects.create(project=project, user=user2, project_role=editor_role)

    # 写入缓存
    permission_service.has_project_permission(user1, project, "project.view")
    permission_service.has_project_permission(user2, project, "project.view")

    # 修改角色权限
    editor_role.permissions = ["project.view", "custom.permission"]
    editor_role.save()

    # 批量失效
    permission_service.invalidate_project_role_cache(project.id, editor_role.id)

    # 缓存应被清除
    cache_key1 = f"project_perms:{project.id}:{user1.id}"
    cache_key2 = f"project_perms:{project.id}:{user2.id}"
    assert cache.get(cache_key1) is None
    assert cache.get(cache_key2) is None
```

- [ ] **Step 5: 运行测试**

```bash
cd backend && pytest apps/accounts/tests/test_project_permission.py -v
```

Expected: PASS

- [ ] **Step 6: 提交**

```bash
git add backend/apps/accounts/services/permission_service.py backend/apps/accounts/permissions_registry.py backend/apps/accounts/tests/test_project_permission.py
git commit -m "feat(accounts): 扩展权限服务支持动态项目角色

- has_project_permission 支持缓存
- 新增权限缓存失效方法
- 注册项目管理相关权限码

Co-Authored-By: Claude Opus 4.7 <noreply@anthropic.com>"
```

---

## Task 5: 创建项目服务（含角色初始化）

**Files:**
- Create: `backend/apps/projects/services/project_service.py`
- Create: `backend/apps/projects/tests/test_project_service.py`

- [ ] **Step 1: 编写项目服务**

Create `backend/apps/projects/services/project_service.py`:

```python
"""项目服务。"""
from django.db import transaction

from apps.projects.models import Project, ProjectMember
from apps.projects.services.role_service import RoleService


class ProjectService:
    """项目服务。"""

    @staticmethod
    @transaction.atomic
    def create_project(*, name, description, created_by, initial_members=None):
        """创建项目并初始化角色。

        Args:
            name: 项目名称
            description: 项目描述
            created_by: 创建人
            initial_members: 初始成员列表 [{"user_id": 1, "role_code": "viewer"}, ...]

        Returns:
            创建的项目实例
        """
        # 创建项目
        project = Project.objects.create(
            name=name,
            description=description,
            created_by=created_by,
        )

        # 初始化内置角色
        roles = RoleService.initialize_builtin_roles(project, created_by=created_by)
        role_map = {r.code: r for r in roles}

        # 创建者自动成为 owner
        owner_role = role_map["owner"]
        ProjectMember.objects.create(
            project=project,
            user=created_by,
            project_role=owner_role,
            added_by=created_by,
        )

        # 添加初始成员
        if initial_members:
            for member_data in initial_members:
                user_id = member_data.get("user_id")
                role_code = member_data.get("role_code", "viewer")
                role = role_map.get(role_code)
                if role and user_id:
                    from apps.accounts.models import User
                    try:
                        user = User.objects.get(pk=user_id)
                        if user != created_by:  # 避免重复添加创建者
                            ProjectMember.objects.create(
                                project=project,
                                user=user,
                                project_role=role,
                                added_by=created_by,
                            )
                    except User.DoesNotExist:
                        pass

        return project

    @staticmethod
    def get_user_projects(user):
        """获取用户参与的项目列表。

        Args:
            user: 用户实例

        Returns:
            项目 QuerySet
        """
        return Project.objects.filter(members__user=user).distinct()

    @staticmethod
    def is_project_member(user, project):
        """检查用户是否是项目成员。

        Args:
            user: 用户实例
            project: 项目实例

        Returns:
            是否是成员
        """
        return ProjectMember.objects.filter(project=project, user=user).exists()
```

- [ ] **Step 2: 编写项目服务测试**

Create `backend/apps/projects/tests/test_project_service.py`:

```python
import pytest
from apps.projects.services.project_service import ProjectService
from apps.projects.models import Project, ProjectMember


@pytest.mark.django_db
def test_create_project_with_owner(user_factory):
    """测试创建项目，创建者自动成为 owner。"""
    user = user_factory()

    project = ProjectService.create_project(
        name="测试项目",
        description="测试描述",
        created_by=user,
    )

    assert project.id is not None
    assert project.name == "测试项目"

    # 验证角色初始化
    assert project.roles.count() == 4

    # 验证创建者是 owner
    member = ProjectMember.objects.get(project=project, user=user)
    assert member.project_role.code == "owner"


@pytest.mark.django_db
def test_create_project_with_initial_members(user_factory):
    """测试创建项目并添加初始成员。"""
    creator = user_factory()
    member1 = user_factory()
    member2 = user_factory()

    project = ProjectService.create_project(
        name="测试项目",
        description="",
        created_by=creator,
        initial_members=[
            {"user_id": member1.id, "role_code": "editor"},
            {"user_id": member2.id, "role_code": "viewer"},
        ],
    )

    # 验证成员数量（创建者 + 2 个初始成员）
    assert project.members.count() == 3

    # 验证成员角色
    m1 = ProjectMember.objects.get(project=project, user=member1)
    assert m1.project_role.code == "editor"

    m2 = ProjectMember.objects.get(project=project, user=member2)
    assert m2.project_role.code == "viewer"


@pytest.mark.django_db
def test_get_user_projects(user_factory, project_factory):
    """测试获取用户参与的项目列表。"""
    user = user_factory()

    # 用户创建的项目
    p1 = project_factory(created_by=user)
    from apps.projects.services.role_service import RoleService
    RoleService.initialize_builtin_roles(p1)
    owner_role = p1.roles.get(code="owner")
    ProjectMember.objects.create(project=p1, user=user, project_role=owner_role)

    # 用户不是成员的项目
    other_user = user_factory()
    p2 = project_factory(created_by=other_user)
    RoleService.initialize_builtin_roles(p2)

    projects = ProjectService.get_user_projects(user)
    assert projects.count() == 1
    assert p1 in projects
    assert p2 not in projects
```

- [ ] **Step 3: 运行测试**

```bash
cd backend && pytest apps/projects/tests/test_project_service.py -v
```

Expected: PASS

- [ ] **Step 4: 提交**

```bash
git add backend/apps/projects/services/project_service.py backend/apps/projects/tests/test_project_service.py
git commit -m "feat(projects): 新增项目服务，创建项目时自动初始化角色

Co-Authored-By: Claude Opus 4.7 <noreply@anthropic.com>"
```

---

## Task 6: 创建项目 API 视图

**Files:**
- Create: `backend/apps/projects/serializers/__init__.py`
- Create: `backend/apps/projects/serializers/project_serializer.py`
- Create: `backend/apps/projects/serializers/role_serializer.py`
- Create: `backend/apps/projects/serializers/member_serializer.py`
- Create: `backend/apps/projects/views/__init__.py`
- Create: `backend/apps/projects/views/project_views.py`
- Create: `backend/apps/projects/views/role_views.py`
- Create: `backend/apps/projects/views/member_views.py`
- Modify: `backend/apps/projects/urls.py`
- Create: `backend/apps/projects/tests/test_project_api.py`

- [ ] **Step 1: 创建序列化器包**

```bash
mkdir -p backend/apps/projects/serializers
touch backend/apps/projects/serializers/__init__.py
```

- [ ] **Step 2: 编写项目序列化器**

Create `backend/apps/projects/serializers/project_serializer.py`:

```python
from rest_framework import serializers

from apps.projects.models import Project


class ProjectSerializer(serializers.ModelSerializer):
    """项目序列化器。"""

    created_by_name = serializers.CharField(source="created_by.real_name", read_only=True)
    member_count = serializers.SerializerMethodField()
    lot_count = serializers.SerializerMethodField()

    class Meta:
        model = Project
        fields = [
            "id", "name", "description", "status",
            "created_by", "created_by_name",
            "member_count", "lot_count",
            "created_at", "updated_at",
        ]
        read_only_fields = ["id", "created_by", "created_at", "updated_at"]

    def get_member_count(self, obj):
        return obj.members.count()

    def get_lot_count(self, obj):
        return obj.lots.count()


class ProjectCreateSerializer(serializers.Serializer):
    """创建项目序列化器。"""

    name = serializers.CharField(max_length=255)
    description = serializers.CharField(required=False, allow_blank=True, default="")
    initial_members = serializers.ListField(
        child=serializers.DictField(),
        required=False,
        default=list,
    )


class ProjectListSerializer(serializers.ModelSerializer):
    """项目列表序列化器（含聚合信息）。"""

    created_by_name = serializers.CharField(source="created_by.real_name", read_only=True)
    lot_count = serializers.IntegerField(read_only=True, default=0)
    progress_pct = serializers.FloatField(read_only=True, default=0)

    class Meta:
        model = Project
        fields = [
            "id", "name", "status",
            "created_by_name", "lot_count", "progress_pct",
            "created_at",
        ]
```

- [ ] **Step 3: 编写角色序列化器**

Create `backend/apps/projects/serializers/role_serializer.py`:

```python
from rest_framework import serializers

from apps.projects.models import ProjectRole


class ProjectRoleSerializer(serializers.ModelSerializer):
    """项目角色序列化器。"""

    member_count = serializers.SerializerMethodField()

    class Meta:
        model = ProjectRole
        fields = [
            "id", "name", "code", "permissions",
            "is_builtin", "is_default", "member_count",
            "created_at", "updated_at",
        ]
        read_only_fields = ["id", "is_builtin", "created_at", "updated_at"]

    def get_member_count(self, obj):
        return obj.members.count()


class ProjectRoleUpdateSerializer(serializers.ModelSerializer):
    """更新角色权限序列化器。"""

    class Meta:
        model = ProjectRole
        fields = ["permissions"]


class ProjectRoleCreateSerializer(serializers.ModelSerializer):
    """创建自定义角色序列化器。"""

    class Meta:
        model = ProjectRole
        fields = ["name", "code", "permissions"]
```

- [ ] **Step 4: 编写成员序列化器**

Create `backend/apps/projects/serializers/member_serializer.py`:

```python
from rest_framework import serializers

from apps.projects.models import ProjectMember


class ProjectMemberSerializer(serializers.ModelSerializer):
    """项目成员序列化器。"""

    user_id = serializers.IntegerField(source="user.id", read_only=True)
    username = serializers.CharField(source="user.username", read_only=True)
    real_name = serializers.CharField(source="user.real_name", read_only=True)
    role_name = serializers.CharField(source="project_role.name", read_only=True)
    role_code = serializers.CharField(source="project_role.code", read_only=True)

    class Meta:
        model = ProjectMember
        fields = [
            "id", "user_id", "username", "real_name",
            "project_role", "role_name", "role_code",
            "created_at",
        ]
        read_only_fields = ["id", "created_at"]


class ProjectMemberCreateSerializer(serializers.Serializer):
    """添加成员序列化器。"""

    user_id = serializers.IntegerField()
    role_id = serializers.IntegerField()


class ProjectMemberUpdateSerializer(serializers.Serializer):
    """更新成员角色序列化器。"""

    role_id = serializers.IntegerField()


class BatchMemberCreateSerializer(serializers.Serializer):
    """批量添加成员序列化器。"""

    members = serializers.ListField(
        child=serializers.DictField(),
        allow_empty=False,
    )
```

- [ ] **Step 5: 创建视图包**

```bash
mkdir -p backend/apps/projects/views
touch backend/apps/projects/views/__init__.py
```

- [ ] **Step 6: 编写项目视图**

Create `backend/apps/projects/views/project_views.py`:

```python
from django.db.models import Count
from rest_framework import status, viewsets
from rest_framework.decorators import action
from rest_framework.permissions import IsAuthenticated
from rest_framework.response import Response

from apps.accounts.permissions import MustChangePasswordPermission
from apps.accounts.services import permission_service
from apps.common.exceptions import NotFound, PermissionDenied
from apps.projects.models import Project
from apps.projects.serializers.project_serializer import (
    ProjectCreateSerializer,
    ProjectListSerializer,
    ProjectSerializer,
)
from apps.projects.services.project_service import ProjectService


class ProjectViewSet(viewsets.ModelViewSet):
    """项目视图集。"""

    permission_classes = [IsAuthenticated, MustChangePasswordPermission]

    def get_queryset(self):
        """只返回用户参与的项目。"""
        queryset = Project.objects.filter(
            members__user=self.request.user
        ).select_related("created_by").prefetch_related("members", "lots")

        # 状态筛选
        status_filter = self.request.query_params.get("status")
        if status_filter:
            queryset = queryset.filter(status=status_filter)

        # 关键词搜索
        keyword = self.request.query_params.get("keyword")
        if keyword:
            queryset = queryset.filter(name__icontains=keyword)

        # 聚合统计
        queryset = queryset.annotate(
            lot_count=Count("lots", distinct=True),
        )

        return queryset.order_by("-created_at")

    def get_serializer_class(self):
        if self.action == "create":
            return ProjectCreateSerializer
        if self.action == "list":
            return ProjectListSerializer
        return ProjectSerializer

    def create(self, request, *args, **kwargs):
        """创建项目。"""
        if not permission_service.has_permission(request.user, "project.create"):
            raise PermissionDenied(message="无创建项目权限")

        serializer = self.get_serializer(data=request.data)
        serializer.is_valid(raise_exception=True)

        project = ProjectService.create_project(
            name=serializer.validated_data["name"],
            description=serializer.validated_data.get("description", ""),
            created_by=request.user,
            initial_members=serializer.validated_data.get("initial_members", []),
        )

        return Response(
            ProjectSerializer(project).data,
            status=status.HTTP_201_CREATED,
        )

    def update(self, request, *args, **kwargs):
        """更新项目。"""
        project = self.get_object()
        if not permission_service.has_project_permission(
            request.user, project, "project.update"
        ):
            raise PermissionDenied(message="无更新项目权限")

        return super().update(request, *args, **kwargs)

    def destroy(self, request, *args, **kwargs):
        """归档项目。"""
        project = self.get_object()
        if not permission_service.has_project_permission(
            request.user, project, "project.delete"
        ):
            raise PermissionDenied(message="无删除项目权限")

        project.status = "archived"
        project.save()
        return Response(status=status.HTTP_204_NO_CONTENT)

    @action(detail=True, methods=["get"])
    def my_permissions(self, request, pk=None):
        """获取当前用户在项目的权限列表。"""
        project = self.get_object()
        permissions = sorted(
            permission_service.get_project_permissions(request.user, project)
        )
        return Response({"project_id": project.id, "permissions": permissions})
```

- [ ] **Step 7: 编写角色视图**

Create `backend/apps/projects/views/role_views.py`:

```python
from rest_framework import status, viewsets
from rest_framework.permissions import IsAuthenticated
from rest_framework.response import Response

from apps.accounts.permissions import MustChangePasswordPermission
from apps.accounts.services import permission_service
from apps.common.exceptions import BadRequest, PermissionDenied
from apps.projects.models import Project, ProjectRole
from apps.projects.serializers.role_serializer import (
    ProjectRoleCreateSerializer,
    ProjectRoleSerializer,
    ProjectRoleUpdateSerializer,
)
from apps.projects.services.role_service import RoleService


class ProjectRoleViewSet(viewsets.ModelViewSet):
    """项目角色视图集。"""

    permission_classes = [IsAuthenticated, MustChangePasswordPermission]
    serializer_class = ProjectRoleSerializer

    def get_queryset(self):
        """返回项目的角色列表。"""
        project_id = self.kwargs.get("project_pk")
        return ProjectRole.objects.filter(project_id=project_id).order_by("code")

    def get_serializer_class(self):
        if self.action == "create":
            return ProjectRoleCreateSerializer
        if self.action == "update" or self.action == "partial_update":
            return ProjectRoleUpdateSerializer
        return ProjectRoleSerializer

    def perform_create(self, serializer):
        """创建自定义角色。"""
        project_id = self.kwargs.get("project_pk")
        project = Project.objects.get(pk=project_id)

        if not permission_service.has_project_permission(
            self.request.user, project, "project.role.manage"
        ):
            raise PermissionDenied(message="无管理角色权限")

        serializer.save(project=project, created_by=self.request.user)

    def update(self, request, *args, **kwargs):
        """更新角色权限。"""
        role = self.get_object()

        if not permission_service.has_project_permission(
            request.user, role.project, "project.role.manage"
        ):
            raise PermissionDenied(message="无管理角色权限")

        serializer = self.get_serializer(role, data=request.data, partial=True)
        serializer.is_valid(raise_exception=True)
        serializer.save()

        # 失效缓存
        from apps.accounts.services.permission_service import invalidate_project_role_cache
        invalidate_project_role_cache(role.project_id, role.id)

        return Response(ProjectRoleSerializer(role).data)

    def destroy(self, request, *args, **kwargs):
        """删除自定义角色。"""
        role = self.get_object()

        if not permission_service.has_project_permission(
            request.user, role.project, "project.role.manage"
        ):
            raise PermissionDenied(message="无管理角色权限")

        if not RoleService.can_delete_role(role):
            raise BadRequest(message="该角色不可删除（内置角色或有成员关联）")

        return super().destroy(request, *args, **kwargs)
```

- [ ] **Step 8: 编写成员视图**

Create `backend/apps/projects/views/member_views.py`:

```python
from rest_framework import status, viewsets
from rest_framework.decorators import action
from rest_framework.permissions import IsAuthenticated
from rest_framework.response import Response

from apps.accounts.models import User
from apps.accounts.permissions import MustChangePasswordPermission
from apps.accounts.services import permission_service
from apps.accounts.services.permission_service import invalidate_user_project_permission_cache
from apps.common.exceptions import BadRequest, NotFound, PermissionDenied
from apps.projects.models import Project, ProjectMember
from apps.projects.serializers.member_serializer import (
    BatchMemberCreateSerializer,
    ProjectMemberCreateSerializer,
    ProjectMemberSerializer,
    ProjectMemberUpdateSerializer,
)


class ProjectMemberViewSet(viewsets.ModelViewSet):
    """项目成员视图集。"""

    permission_classes = [IsAuthenticated, MustChangePasswordPermission]
    serializer_class = ProjectMemberSerializer

    def get_queryset(self):
        """返回项目的成员列表。"""
        project_id = self.kwargs.get("project_pk")
        return ProjectMember.objects.filter(
            project_id=project_id
        ).select_related("user", "project_role").order_by("created_at")

    def get_serializer_class(self):
        if self.action == "create":
            return ProjectMemberCreateSerializer
        if self.action == "update" or self.action == "partial_update":
            return ProjectMemberUpdateSerializer
        return ProjectMemberSerializer

    def get_project(self):
        """获取项目实例。"""
        project_id = self.kwargs.get("project_pk")
        try:
            return Project.objects.get(pk=project_id)
        except Project.DoesNotExist:
            raise NotFound(message="项目不存在")

    def create(self, request, *args, **kwargs):
        """添加成员。"""
        project = self.get_project()

        if not permission_service.has_project_permission(
            request.user, project, "project.member.manage"
        ):
            raise PermissionDenied(message="无管理成员权限")

        serializer = self.get_serializer(data=request.data)
        serializer.is_valid(raise_exception=True)

        user_id = serializer.validated_data["user_id"]
        role_id = serializer.validated_data["role_id"]

        try:
            user = User.objects.get(pk=user_id)
        except User.DoesNotExist:
            raise NotFound(message="用户不存在")

        # 检查是否已是成员
        if ProjectMember.objects.filter(project=project, user=user).exists():
            raise BadRequest(message="该用户已是项目成员")

        member = ProjectMember.objects.create(
            project=project,
            user=user,
            project_role_id=role_id,
            added_by=request.user,
        )

        return Response(
            ProjectMemberSerializer(member).data,
            status=status.HTTP_201_CREATED,
        )

    def update(self, request, *args, **kwargs):
        """更新成员角色。"""
        member = self.get_object()
        project = member.project

        if not permission_service.has_project_permission(
            request.user, project, "project.member.manage"
        ):
            raise PermissionDenied(message="无管理成员权限")

        # 不能修改 owner 的角色（防止 owner lockout）
        if member.project_role.code == "owner":
            raise BadRequest(message="不能修改负责人的角色")

        serializer = self.get_serializer(data=request.data)
        serializer.is_valid(raise_exception=True)

        old_role_id = member.project_role_id
        member.project_role_id = serializer.validated_data["role_id"]
        member.save()

        # 失效缓存
        invalidate_user_project_permission_cache(project.id, member.user_id)

        return Response(ProjectMemberSerializer(member).data)

    def destroy(self, request, *args, **kwargs):
        """移除成员。"""
        member = self.get_object()
        project = member.project

        if not permission_service.has_project_permission(
            request.user, project, "project.member.manage"
        ):
            raise PermissionDenied(message="无管理成员权限")

        # 不能移除 owner
        if member.project_role.code == "owner":
            raise BadRequest(message="不能移除项目负责人")

        user_id = member.user_id
        member.delete()

        # 失效缓存
        invalidate_user_project_permission_cache(project.id, user_id)

        return Response(status=status.HTTP_204_NO_CONTENT)

    @action(detail=False, methods=["post"])
    def batch(self, request, project_pk=None):
        """批量添加成员。"""
        project = self.get_project()

        if not permission_service.has_project_permission(
            request.user, project, "project.member.manage"
        ):
            raise PermissionDenied(message="无管理成员权限")

        serializer = BatchMemberCreateSerializer(data=request.data)
        serializer.is_valid(raise_exception=True)

        success = 0
        failed = 0
        results = []

        for member_data in serializer.validated_data["members"]:
            user_id = member_data.get("user_id")
            role_id = member_data.get("role_id")

            try:
                user = User.objects.get(pk=user_id)
                if ProjectMember.objects.filter(project=project, user=user).exists():
                    results.append({"user_id": user_id, "status": "already_member"})
                    failed += 1
                    continue

                ProjectMember.objects.create(
                    project=project,
                    user=user,
                    project_role_id=role_id,
                    added_by=request.user,
                )
                results.append({"user_id": user_id, "status": "success"})
                success += 1
            except Exception:
                results.append({"user_id": user_id, "status": "failed"})
                failed += 1

        return Response({
            "success": success,
            "failed": failed,
            "results": results,
        })
```

- [ ] **Step 9: 更新 URL 路由**

Edit `backend/apps/projects/urls.py`:

```python
from django.urls import include, path
from rest_framework.routers import DefaultRouter

from apps.projects.views.member_views import ProjectMemberViewSet
from apps.projects.views.project_views import ProjectViewSet
from apps.projects.views.role_views import ProjectRoleViewSet

router = DefaultRouter()
router.register(r"projects", ProjectViewSet, basename="project")

urlpatterns = [
    path("", include(router.urls)),
    path(
        "projects/<int:project_pk>/roles/",
        ProjectRoleViewSet.as_view({"get": "list", "post": "create"}),
        name="project-roles",
    ),
    path(
        "projects/<int:project_pk>/roles/<int:pk>/",
        ProjectRoleViewSet.as_view({
            "get": "retrieve",
            "put": "update",
            "patch": "partial_update",
            "delete": "destroy",
        }),
        name="project-role-detail",
    ),
    path(
        "projects/<int:project_pk>/members/",
        ProjectMemberViewSet.as_view({"get": "list", "post": "create"}),
        name="project-members",
    ),
    path(
        "projects/<int:project_pk>/members/<int:pk>/",
        ProjectMemberViewSet.as_view({
            "get": "retrieve",
            "put": "update",
            "patch": "partial_update",
            "delete": "destroy",
        }),
        name="project-member-detail",
    ),
    path(
        "projects/<int:project_pk>/members/batch/",
        ProjectMemberViewSet.as_view({"post": "batch"}),
        name="project-members-batch",
    ),
]
```

- [ ] **Step 10: 编写 API 测试**

Create `backend/apps/projects/tests/test_project_api.py`:

```python
import pytest
from django.urls import reverse
from rest_framework.test import APIClient

from apps.projects.models import Project, ProjectMember
from apps.projects.services.role_service import RoleService


@pytest.fixture
def api_client():
    return APIClient()


@pytest.fixture
def authenticated_client(api_client, user_factory):
    user = user_factory()
    api_client.force_authenticate(user=user)
    return api_client, user


@pytest.mark.django_db
def test_create_project(authenticated_client):
    """测试创建项目。"""
    client, user = authenticated_client

    response = client.post("/api/projects/", {
        "name": "新项目",
        "description": "测试描述",
    })

    assert response.status_code == 201
    assert response.data["name"] == "新项目"

    # 验证创建者是 owner
    project = Project.objects.get(pk=response.data["id"])
    member = ProjectMember.objects.get(project=project, user=user)
    assert member.project_role.code == "owner"


@pytest.mark.django_db
def test_list_projects(authenticated_client, project_factory, user_factory):
    """测试获取项目列表。"""
    client, user = authenticated_client

    # 创建项目
    project = project_factory(created_by=user)
    roles = RoleService.initialize_builtin_roles(project)
    owner_role = next(r for r in roles if r.code == "owner")
    ProjectMember.objects.create(project=project, user=user, project_role=owner_role)

    response = client.get("/api/projects/")

    assert response.status_code == 200
    assert len(response.data["results"]) == 1


@pytest.mark.django_db
def test_add_member(authenticated_client, user_factory):
    """测试添加成员。"""
    client, owner = authenticated_client

    # 创建项目
    project = Project.objects.create(name="测试项目", created_by=owner)
    roles = RoleService.initialize_builtin_roles(project)
    owner_role = next(r for r in roles if r.code == "owner")
    ProjectMember.objects.create(project=project, user=owner, project_role=owner_role)

    # 添加成员
    new_user = user_factory()
    viewer_role = next(r for r in roles if r.code == "viewer")

    response = client.post(f"/api/projects/{project.id}/members/", {
        "user_id": new_user.id,
        "role_id": viewer_role.id,
    })

    assert response.status_code == 201
    assert ProjectMember.objects.filter(project=project, user=new_user).exists()


@pytest.mark.django_db
def test_remove_member(authenticated_client, user_factory):
    """测试移除成员。"""
    client, owner = authenticated_client

    # 创建项目和成员
    project = Project.objects.create(name="测试项目", created_by=owner)
    roles = RoleService.initialize_builtin_roles(project)
    owner_role = next(r for r in roles if r.code == "owner")
    editor_role = next(r for r in roles if r.code == "editor")

    ProjectMember.objects.create(project=project, user=owner, project_role=owner_role)

    editor = user_factory()
    member = ProjectMember.objects.create(project=project, user=editor, project_role=editor_role)

    # 移除成员
    response = client.delete(f"/api/projects/{project.id}/members/{member.id}/")

    assert response.status_code == 204
    assert not ProjectMember.objects.filter(project=project, user=editor).exists()


@pytest.mark.django_db
def test_cannot_remove_owner(authenticated_client):
    """测试不能移除 owner。"""
    client, owner = authenticated_client

    project = Project.objects.create(name="测试项目", created_by=owner)
    roles = RoleService.initialize_builtin_roles(project)
    owner_role = next(r for r in roles if r.code == "owner")
    member = ProjectMember.objects.create(project=project, user=owner, project_role=owner_role)

    response = client.delete(f"/api/projects/{project.id}/members/{member.id}/")

    assert response.status_code == 400
```

- [ ] **Step 11: 运行测试**

```bash
cd backend && pytest apps/projects/tests/test_project_api.py -v
```

Expected: PASS

- [ ] **Step 12: 提交**

```bash
git add backend/apps/projects/serializers/ backend/apps/projects/views/ backend/apps/projects/urls.py backend/apps/projects/tests/test_project_api.py
git commit -m "feat(projects): 新增项目/角色/成员 API

- ProjectViewSet: 项目 CRUD
- ProjectRoleViewSet: 角色管理
- ProjectMemberViewSet: 成员管理（含批量操作）
- 权限校验集成

Co-Authored-By: Claude Opus 4.7 <noreply@anthropic.com>"
```

---

## Self-Review

### 1. Spec Coverage Check

| 需求 | 任务 |
|------|------|
| ProjectRole 模型 | Task 1 |
| ProjectMember 改为 FK | Task 2 |
| 角色权限缓存 | Task 4 |
| Owner Lockout 防护 | Task 1 (模型 save 方法) |
| 角色服务 | Task 3 |
| 项目服务（含初始化） | Task 5 |
| 项目 API | Task 6 |
| 角色 API | Task 6 |
| 成员 API | Task 6 |
| 批量操作 API | Task 6 |

### 2. Placeholder Scan

- ✅ 无 "TBD"、"TODO"、"implement later"
- ✅ 所有代码步骤都有完整代码
- ✅ 所有命令都有预期输出

### 3. Type Consistency

- ✅ ProjectRole.code 在所有地方都使用字符串
- ✅ permission_service 方法签名一致
- ✅ 序列化器字段名与模型字段一致

---

## Execution Handoff

**Plan complete and saved to `docs/superpowers/plans/2026-05-22-phase4.1-project-role.md`. Two execution options:**

**1. Subagent-Driven (recommended)** - 我为每个任务派发独立子代理，任务间审查，快速迭代

**2. Inline Execution** - 在当前会话中使用 executing-plans 执行，批量执行带检查点

**选择哪种方式？**
